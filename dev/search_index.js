var documenterSearchIndex = {"docs":
[{"category":"section","location":"guide/#Guide","page":"Guide","text":"The examples below assume using MeasurementSets and a Measurement Set at /path/to/my.ms. They are illustrative, not doctested.","title":"Guide"},{"category":"section","location":"guide/#Opening-a-Measurement-Set","page":"Guide","text":"ms = MeasurementSet(\"/path/to/my.ms\")\nsubtablenames(ms)                        # [\"ANTENNA\", \"SPECTRAL_WINDOW\", …]\n\nt = readtable(\"/path/to/my.ms\")          # the MAIN table on its own\nnrow(t)\ncolumnnames(t)\n\nreadtable also opens a single subtable directory (readtable(\"/path/to/my.ms/ANTENNA\")) or any casacore table.","title":"Opening a Measurement Set"},{"category":"section","location":"guide/#Reading-columns","page":"Guide","text":"column returns a lazy Column (<: AbstractVector):\n\nms[:DATA][42]                            # one cell: a 4×64 ComplexF32\nms[:UVW][1:100]                          # first 100 baselines' UVW\ncolumn(ms.data, \"TIME\")[:]               # the whole column (fast path)\n\nColumn and table metadata:\n\ncolumndesc(t, \"DATA\")                    # a ColumnDesc (type, shape, keywords, DM binding)\nkeywords(t)[\"MS_VERSION\"]                # 2.0f0\ncolumndesc(t, \"UVW\").keywords            # per-column keywords (units, MEASINFO)","title":"Reading columns"},{"category":"section","location":"guide/#Precision","page":"Guide","text":"A MAIN table's TpComplex visibility columns — DATA, MODEL_DATA, CORRECTED_DATA — read back as ComplexF16 by default. The on-disk bytes are ComplexF32; the visibilities derive from 8-bit samples, so nothing real is lost and the working set halves.\n\neltype(column(t, \"DATA\"))                     # Matrix{ComplexF16}\neltype(column(t, \"DATA\"; precision=:full))    # Matrix{ComplexF32}\nreadtable(\"my.ms\"; precision=:full)           # every column wide\nreadtable(\"my.ms\"; precision=BFloat16)        # DATA + WEIGHT + SIGMA all 16-bit\nMeasurementSet(\"my.ms\"; precision=:full)\n\nTpFloat columns (WEIGHT, SIGMA, WEIGHT_SPECTRUM) stay Float32 under the default and under precision=Float16 (real weights exceed Float16's 65504 range). Pass precision=BFloat16 to narrow them too — BFloat16 has Float32's exponent range (overflow-safe) and a 7-bit mantissa that matches 8-bit-derived data; DATA then comes back as Complex{BFloat16} and WEIGHT as BFloat16.\n\nFloat64 (TIME, UVW), ComplexF64 and Bool are never narrowed. Non-MAIN tables default to :full. copyms / copytable / edit always read at full precision, so copies and in-place edits stay byte-exact; a narrowed column written back upcasts to Float32 on disk.","title":"Precision"},{"category":"section","location":"guide/#Tables.jl-interop","page":"Guide","text":"Every table is a Tables.jl source — column and row access — so subtables drop straight into the data ecosystem:\n\nusing DataFrames\nDataFrame(subtable(ms, \"ANTENNA\"))\nTables.schema(subtable(ms, \"SPECTRAL_WINDOW\"))\nTables.rowtable(subtable(ms, \"FIELD\"))","title":"Tables.jl interop"},{"category":"section","location":"guide/#Validating-against-the-standard-schema","page":"Guide","text":"The MS v2 standard schema (NRAO Memo 229) is available as data:\n\nvalidate(ms)                             # String[] when conformant; never throws\nstdtable(\"SPECTRAL_WINDOW\").columns\nstdcolumns(\"ANTENNA\")","title":"Validating against the standard schema"},{"category":"section","location":"guide/#Reference-frames","page":"Guide","text":"measure(t, col[, row]) reads a measure-valued column as a typed value with its reference frame; import SOFA (+ optionally EarthOrientation) converts between frames.\n\nimport SOFA, EarthOrientation\n\ne = measure(main, \"TIME\", 1)                       # MEpoch{UTC}\nmeasconvert(e, TT)                                 # → MEpoch{TT}\n\nfr = MeasFrame(epoch = e,\n               position = measure(subtable(ms, \"ANTENNA\"), \"POSITION\", 1),\n               direction = measure(subtable(ms, \"FIELD\"), \"PHASE_DIR\", 1))\nmeasconvert(MDirection{J2000}(2.0, 0.5), AZEL; frame = fr)\nmeasconvert(measure(subtable(ms, \"SPECTRAL_WINDOW\"), \"CHAN_FREQ\", 1)[1],\n            LSRK; frame = fr)\n\nSee Concepts for the full frame list, accuracy notes, and the measures = write keyword.","title":"Reference frames"},{"category":"section","location":"guide/#Writing-and-copying","page":"Guide","text":"copyms(\"/path/to/my.ms\", \"/tmp/copy.ms\"; rows = 1:2000)\ncreate_ms(\"/tmp/synth.ms\"; nrow = 100, nchan = 64, ncorr = 4, nant = 6)\n\nwrite_table(\"/tmp/spw\", \"SPECTRAL_WINDOW\",\n            [\"NUM_CHAN\"  => [64, 32],\n             \"CHAN_FREQ\" => [collect(1.0:64.0), collect(1.0:32.0)]];  # ragged\n            nrow = 2)\n\ncopyms / copytable preserve each column's storage-manager, virtual engine and Dysco compression. write_table / create_ms pick StandardStMan by default; tsm= / ism= / engines= / dysco= choose a different layout, and storage=:multifile / :multihdf5 packs the result into one container file.\n\ncopytable is also SELECT … INTO — it materialises any query result:\n\ncopytable(\"/tmp/cal.tab\", query(ms.MAIN, \"FIELD_ID == 3\"))","title":"Writing and copying"},{"category":"section","location":"guide/#Editing-in-place","page":"Guide","text":"edit(\"/tmp/copy.ms\") do t\n    t[:FLAG][5] = trues(4, 64)           # a tiled cell -- patched in the tile file\n    t[:SCAN_NUMBER][10] = 7\n    addrows!(t, 10)                      # every storage manager's row count grows\n    for r in 91:100\n        t[:TIME][r] = 4.6e9 + r\n    end\nend\n\nRow and schema mutation take a whole-file regeneration path:\n\nedit(\"/tmp/copy.ms\") do t\n    removerows!(t, [2, 5, 9])\n    addcolumn!(t, \"WEIGHT_SPECTRUM\")\n    removecolumn!(t, \"FLAG_CATEGORY\")\nend\n\nThe flush runs under an exclusive table.lock and updates the sync blob, so a concurrent casacore reader re-syncs. is_stale / resync let a long-lived reader notice an external write.","title":"Editing in place"},{"category":"section","location":"guide/#Querying","page":"Guide","text":"query row-filters a table and returns a RefTable:\n\nsel = query(ms.MAIN, \"ANTENNA1 != ANTENNA2 AND mean(abs(DATA)) > 3 ORDER BY TIME\")\n\nThe WHERE string supports comparisons, AND/OR/NOT (or &&/||/!), parentheses, IN [...], arithmetic (+ - * / % // **), LIKE / ~ p/glob/ / ~ m/regex/ pattern matching, a function library (abs, sqrt, mean, any, rownumber(), …), and a trailing ORDER BY. There is also a closure form:\n\nquery(ms.MAIN; cols = [\"ANTENNA1\", \"ANTENNA2\", \"UVW\"]) do row\n    hypot(row.UVW...) > 500.0\nend\n\ngroupby computes one row per group:\n\ngroupby(ms.MAIN, \"FIELD_ID\";\n        select = [\"FIELD_ID\" => :FIELD_ID,\n                  \"N\"        => \"gcount()\",\n                  \"AMP\"      => \"gmean(mean(abs(DATA)))\"],\n        having = \"gcount() > 100\",\n        orderby = [\"AMP\" => :desc])\n\nor, for aggregates the g* set can't express, a do-block returning a NamedTuple:\n\ngroupby(ms.MAIN, [:ANTENNA1]; cols = [\"ANTENNA1\", \"DATA\", \"WEIGHT\"]) do g\n    (; ANT  = first(g.ANTENNA1),\n       N    = length(g),\n       WAMP = sum(mean.(abs, g.DATA) .* g.WEIGHT) / sum(g.WEIGHT))\nend\n\njoin (a method added to Base.join) attaches a subtable's columns to each MAIN row — an N:1 lookup join, TaQL's own JOIN … ON semantics:\n\njoin(ms.MAIN, subtable(ms, \"ANTENNA\");\n     on        = \"ANTENNA1\",                    # 0-based row index into ANTENNA\n     rightcols = [\"NAME\" => \"ANT_NAME\"],\n     where     = \"ANT_NAME ~ p/DA*/\")\n\non is a column name (that left column is a 0-based row index into the right table), a \"LKEY\" => \"RKEY\" pair (equi-join), or a vector of pairs (composite key). unmatched is :error (default), :drop or :missing.\n\nEvery result is an AbstractTable, so the verbs chain, and any result persists with write_table(dst, \"T\", result; nrow = nrow(result)) or copytable.","title":"Querying"},{"category":"section","location":"guide/#Row-level-write-commands","page":"Guide","text":"update!, delete! (extends Base.delete!) and insert! (extends Base.insert!) change a table in place:\n\nupdate!(\"/tmp/copy.ms/ANTENNA\";\n        set = [\"MOUNT\" => \"'ALT-AZ'\"], where = \"STATION ~ p/PM*/\")\n\ndelete!(\"/tmp/copy.ms/FLAG_CMD\"; where = \"APPLIED\")\n\ninsert!(\"/tmp/copy.ms/STATE\"; values = (; OBS_MODE = \"CALIBRATE_PHASE\", SIG = true))\ninsert!(\"/tmp/dst.ms/STATE\", query(other, \"OBS_MODE == 'CALIBRATE_PHASE'\"))\n\nupdate!'s set RHS are TaQL-lite expressions evaluated against the pre-update row (so [\"A\" => \"B\", \"B\" => \"A\"] swaps). where is a WHERE string, a row -> Bool closure, or nothing (every row).\n\ntaql wraps all four as one string dispatcher:\n\ntaql(\"/tmp/copy.ms\", \"UPDATE t SET UVW = UVW * 2 WHERE ANTENNA1 == 0\")\ntaql(\"/tmp/copy.ms\", \"DELETE FROM t WHERE FLAG_ROW\")\ntaql(\"/tmp/copy.ms\", \"INSERT INTO t (A, B) VALUES (1, 2.5), (3, 4.5)\")\ntaql(ms.MAIN,        \"SELECT A, B AS BB WHERE A > 5 INTO '/tmp/out'\")","title":"Row-level write commands"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"\n\njoin, delete! and insert! below are methods added to Base.join / Base.delete! / Base.insert! (an N:1 lookup join and the DELETE / INSERT write commands); they are not exported.\n\nBFloat16 is re-exported from BFloat16s.jl so that readtable(ms; precision = BFloat16) works after using MeasurementSets.","title":"API reference"},{"category":"section","location":"api/#Opening-tables","page":"API reference","text":"","title":"Opening tables"},{"category":"section","location":"api/#Table-types","page":"API reference","text":"","title":"Table types"},{"category":"section","location":"api/#Columns-and-data","page":"API reference","text":"","title":"Columns and data"},{"category":"section","location":"api/#Physical-units","page":"API reference","text":"import Unitful, UnitfulAngles, UnitfulAstro gives these real methods (see Concepts).","title":"Physical units"},{"category":"section","location":"api/#Reference-frames-(measures)","page":"API reference","text":"import SOFA (and optionally EarthOrientation) activates measconvert; see Concepts.","title":"Reference frames (measures)"},{"category":"section","location":"api/#Schema-and-metadata","page":"API reference","text":"","title":"Schema and metadata"},{"category":"section","location":"api/#The-standard-schema","page":"API reference","text":"","title":"The standard schema"},{"category":"section","location":"api/#Writing-tables","page":"API reference","text":"","title":"Writing tables"},{"category":"section","location":"api/#Editing-in-place","page":"API reference","text":"","title":"Editing in place"},{"category":"section","location":"api/#Concurrency","page":"API reference","text":"","title":"Concurrency"},{"category":"section","location":"api/#Query-engine","page":"API reference","text":"","title":"Query engine"},{"category":"function","location":"api/#MeasurementSets.readtable","page":"API reference","text":"readtable(path) -> Table | RefTable | ConcatTable\n\nRead the metadata (description, keywords, data-manager bindings, row count) of the casacore table directory at path.  Column data is not read.\n\nReturns a RefTable or ConcatTable when path is a reference / concatenation table (its parent(s) are opened recursively); otherwise a plain Table.\n\nprecision controls the in-memory element type of TpFloat / TpComplex columns (the on-disk Float32 / ComplexF32 bytes are never changed; Float64 / ComplexF64 / Bool are never narrowed):\n\nvalue effect\nnothing (default) :half for a MAIN table (table.info Type \"Measurement Set\"), :full otherwise\n:half narrow TpComplex only → ComplexF16 (DATA, …); TpFloat stays Float32 (real WEIGHT values exceed Float16's 65504 range)\n:full / Float32 no narrowing\nFloat16 narrow every TpFloat→Float16, TpComplex→ComplexF16 (a large WEIGHT will overflow to Inf — caller's risk)\nBFloat16 narrow every TpFloat→BFloat16, TpComplex→Complex{BFloat16} (BFloat16 has Float32's exponent range — overflow-safe, and its 7-bit mantissa matches 8-bit-derived data)\n\nOverride per column with column(t, name; precision=…).\n\nA shared (read) lock on <path>/table.lock is held only while table.dat is slurped; the row count then comes from the table.lock sync blob when present (as casacore does), else from table.dat.  The lazy storage-manager reads that a later column() triggers are not locked – they rely on the writers' atomic renames.\n\n\n\n\n\n","title":"MeasurementSets.readtable"},{"category":"type","location":"api/#MeasurementSets.MeasurementSet","page":"API reference","text":"MeasurementSet(path; precision=nothing) -> MeasurementSet\n\nOpen the MAIN table of a Measurement Set directory and read its metadata. Subtables are read on first access via getproperty / subtable.\n\nprecision is forwarded to readtable for the MAIN table (:half — the default — reads DATA etc. as ComplexF16; :full, Float16 and BFloat16 also accepted). Subtables always open at :full.\n\n\n\n\n\n","title":"MeasurementSets.MeasurementSet"},{"category":"function","location":"api/#MeasurementSets.subtable","page":"API reference","text":"subtable(ms, name) -> Table\n\nRead (and cache) the subtable referenced by keyword name in MAIN.\n\n\n\n\n\n","title":"MeasurementSets.subtable"},{"category":"function","location":"api/#MeasurementSets.subtablenames","page":"API reference","text":"subtablenames(ms) -> Vector{String}\n\nThe names of ms's subtables (\"ANTENNA\", \"SPECTRAL_WINDOW\", …). Open one with subtable(ms, name) or ms.NAME.\n\n\n\n\n\n","title":"MeasurementSets.subtablenames"},{"category":"type","location":"api/#MeasurementSets.AbstractTable","page":"API reference","text":"Common supertype of the casacore table kinds this package can read: Table (a plain table), RefTable (a row-number reference into a parent table), and ConcatTable (a virtual row-wise concatenation of same-schema tables).  All three answer nrow / columnnames / columndesc / keywords / subtables / column and interoperate with Tables.jl.\n\n\n\n\n\n","title":"MeasurementSets.AbstractTable"},{"category":"type","location":"api/#MeasurementSets.Table","page":"API reference","text":"Table <: AbstractTable\n\nA plain on-disk casacore table, as returned by readtable for a table.dat of subtype \"PlainTable\" (the MS MAIN table and every standard subtable). Column data is read lazily and on demand through the bound storage managers; cell reads go through column / getcell / t[:NAME]. See also RefTable and ConcatTable for the reference / concatenation kinds, and edit to open one for update.\n\nprecision (:half / :full / Float16 / BFloat16) — see readtable.\n\n\n\n\n\n","title":"MeasurementSets.Table"},{"category":"type","location":"api/#MeasurementSets.RefTable","page":"API reference","text":"A casacore RefTable: a persistent row-number reference into a parent table (what a TaQL SELECT ... GIVING '<path>' row selection or table.query writes).  Stores no cell data of its own — every read delegates to parent through rows (1-based parent row per ref row).\n\n\n\n\n\n","title":"MeasurementSets.RefTable"},{"category":"type","location":"api/#MeasurementSets.ConcatTable","page":"API reference","text":"A casacore ConcatTable: a virtual row-wise concatenation of parts (same-schema tables — e.g. the MAIN table of a MultiMS).  offsets is the cumulative row map (length(parts)+1 entries, offsets[end] = total rows).  Stores no cell data of its own.\n\n\n\n\n\n","title":"MeasurementSets.ConcatTable"},{"category":"type","location":"api/#MeasurementSets.GroupedTable","page":"API reference","text":"GroupedTable\n\nAn in-memory columnar table — the result of groupby, join, or query on one of those. It is a full AbstractTable (so it chains back into query / groupby / join) and a Tables.jl source. Access columns with gt.OUTNAME, gt[\"OUTNAME\"], column(gt, \"OUTNAME\"), DataFrame(gt), or persist with write_table(dst, \"T\", gt; nrow=nrow(gt)).\n\nnames and cols are reserved field names (reached via getfield); a column literally named names / cols is accessible only through gt[\"names\"] / column(gt, \"cols\").\n\n\n\n\n\n","title":"MeasurementSets.GroupedTable"},{"category":"type","location":"api/#MeasurementSets.SubTable","page":"API reference","text":"A reference to a subtable stored as a TpTable keyword value.\n\n\n\n\n\n","title":"MeasurementSets.SubTable"},{"category":"function","location":"api/#MeasurementSets.column","page":"API reference","text":"column(t::Table, name; precision=nothing) -> Column\n\nA lazy AbstractVector over a column: col[i] reads one cell, col[r] a range, col[:] the whole column (fast path).\n\nprecision (nothing follows t.precision; otherwise :half / :full / Float16 / BFloat16 / Float32 — see readtable): :half narrows only a TpComplex column (to ComplexF16); Float16 / BFloat16 narrow any Float32 / ComplexF32 column to that scalar type / its Complex; :full keeps the column wide.\n\n\n\n\n\n","title":"MeasurementSets.column"},{"category":"type","location":"api/#MeasurementSets.Column","page":"API reference","text":"Column{T} <: AbstractVector{T}\n\nA lazy view of one table column, returned by column and t[:NAME]. Indexing reads on demand: col[i] fetches one cell, col[:] takes the storage manager's whole-column fast path, col[r] a row subset. T is the best-known element type — a scalar, a fixed-shape Array{E,N}, or Array{E} when the rank is only known once a cell is read.\n\n\n\n\n\n","title":"MeasurementSets.Column"},{"category":"method","location":"api/#MeasurementSets.getcolumn-Tuple{AbstractTable, AbstractString}","page":"API reference","text":"getcolumn(t, name; precision=nothing) -> Vector / Vector{Array}\n\nRead an entire column's data (eager; equivalent to column(t, name; precision)[:]).\n\n\n\n\n\n","title":"MeasurementSets.getcolumn"},{"category":"method","location":"api/#MeasurementSets.getcell-Tuple{AbstractTable, AbstractString, Integer}","page":"API reference","text":"getcell(t, name, row; precision=nothing) -> value\n\nRead one cell (row is 1-based).\n\n\n\n\n\n","title":"MeasurementSets.getcell"},{"category":"function","location":"api/#MeasurementSets.nrow","page":"API reference","text":"nrow(t) -> Int\n\nNumber of rows in table t (a Table, RefTable, ConcatTable or GroupedTable).\n\n\n\n\n\n","title":"MeasurementSets.nrow"},{"category":"function","location":"api/#MeasurementSets.columnnames","page":"API reference","text":"columnnames(t) -> Vector{String}\n\nThe column names of table t, in schema order.\n\n\n\n\n\n","title":"MeasurementSets.columnnames"},{"category":"function","location":"api/#MeasurementSets.columnunit","page":"API reference","text":"columnunit(t, name) -> Unitful.Units | Tuple | nothing\n\nThe physical unit of column name, from its QuantumUnits keyword, as a Unitful unit. nothing when the column has no unit keyword; a Tuple for a genuinely mixed-unit column. Needs the Unitful extension — import Unitful, UnitfulAngles, UnitfulAstro.\n\n\n\n\n\n","title":"MeasurementSets.columnunit"},{"category":"function","location":"api/#MeasurementSets.qcolumn","page":"API reference","text":"qcolumn(t, name; precision=nothing) -> Vector\n\nThe whole column with its unit attached as Unitful quantities (materialised, not a lazy Column). A unitless column is returned unchanged. Needs the Unitful extension.\n\n\n\n\n\n","title":"MeasurementSets.qcolumn"},{"category":"constant","location":"api/#MeasurementSets.UNITS_NO_JULIA_COUNTERPART","page":"API reference","text":"UNITS_NO_JULIA_COUNTERPART\n\ncasacore units that no third-party Julia package (Unitful / UnitfulAngles / UnitfulAstro) implements, and how MeasurementSets handles each:\n\n:pseudo — registered as a dimensionless unit by the Unitful extension (so Jy/beam parses to Jy beam⁻¹, dimensionally Jy).\n:nounits — mapped to Unitful.NoUnits (casacore's canonical dimensionless markers).\n:unsupported — raises a clear error; use the suggested replacement.\n:dimension — exists in Unitful but with a different dimension than casacore (casacore treats it as a base dimension).\n\n\n\n\n\n","title":"MeasurementSets.UNITS_NO_JULIA_COUNTERPART"},{"category":"function","location":"api/#MeasurementSets.measinfo","page":"API reference","text":"measinfo(t, col) -> Union{Nothing,MeasInfo}\n\nParse the MEASINFO keyword of column col.  nothing if the column carries no measure information.\n\n\n\n\n\n","title":"MeasurementSets.measinfo"},{"category":"type","location":"api/#MeasurementSets.MeasInfo","page":"API reference","text":"MeasInfo\n\nThe parsed MEASINFO (+ QuantumUnits) of a measure-valued column. fixedref xor varrefcol is set.\n\n\n\n\n\n","title":"MeasurementSets.MeasInfo"},{"category":"function","location":"api/#MeasurementSets.measure","page":"API reference","text":"measure(t, col, row; epoch=nothing) -> Measure | Vector{Measure}\nmeasure(t, col)                      -> Vector\n\nRead column col of t as a physical measure, taking its reference frame from the MEASINFO keyword (a fixed Ref, or a per-row VarRefCol code).  An epoch column yields MEpoch (MJD days), a direction column MDirection (radians), a position column MPosition (metres), a frequency column MFrequency (Hz) – an array-valued frequency/direction cell yields a Vector of them.\n\nPassing epoch (an MEpoch) to a direction cell of a FIELD subtable evaluates a moving target: if that field row is a comet / planet (field_ephemeris finds one), the returned direction is the ephemeris position at epoch (in the ephemeris table's own posrefsys frame), shifted by the stored PHASE_DIR offset.\n\n\n\n\n\n","title":"MeasurementSets.measure"},{"category":"function","location":"api/#MeasurementSets.measconvert","page":"API reference","text":"measconvert(m::Measure, R::Type{<:RefFrame}; frame=MeasFrame()) -> Measure\n\nConvert measure m to reference frame R.  Needs SOFA.jl loaded (import SOFA); import EarthOrientation as well for full ΔUT1 / polar-motion accuracy (otherwise ~1 arcsecond, with a one-time warning). frame supplies whatever auxiliary epoch / position / source direction the target frame requires – see MeasFrame.\n\n\n\n\n\nmeasconvert(d::MDoppler{C}, C2::Type{<:DopplerType}) -> MDoppler{C2}\n\nRe-express a Doppler shift in another convention.\n\n\n\n\n\n","title":"MeasurementSets.measconvert"},{"category":"type","location":"api/#MeasurementSets.MeasFrame","page":"API reference","text":"MeasFrame(; epoch, position, direction)\n\nThe auxiliary information a reference-frame conversion needs: an MEpoch (for APP, AZEL, any velocity frame), an MPosition (for AZEL/HADEC, TOPO), and an MDirection source direction (for every frequency / radial-velocity conversion).  Fields are set as needed and may be left nothing.\n\n\n\n\n\n","title":"MeasurementSets.MeasFrame"},{"category":"type","location":"api/#MeasurementSets.MEpoch","page":"API reference","text":"MEpoch{R}(mjd)\n\nAn instant of time as a Modified Julian Date (days) in time scale R (UTC/TAI/TT/TDB/UT1).\n\n\n\n\n\n","title":"MeasurementSets.MEpoch"},{"category":"type","location":"api/#MeasurementSets.MDirection","page":"API reference","text":"MDirection{R}(lon, lat)\n\nA direction on the sky, longitude/latitude in radians, in frame R (J2000/ICRS/B1950/APP/GALACTIC/ECLIPTIC/AZEL/AZELGEO/HADEC).\n\n\n\n\n\n","title":"MeasurementSets.MDirection"},{"category":"type","location":"api/#MeasurementSets.MPosition","page":"API reference","text":"MPosition{R}(x, y, z)\n\nA location, geocentric Cartesian metres, in frame R (ITRF/WGS84).\n\n\n\n\n\n","title":"MeasurementSets.MPosition"},{"category":"type","location":"api/#MeasurementSets.MFrequency","page":"API reference","text":"MFrequency{R}(hz)\n\nA frequency in hertz, referenced to velocity frame R (TOPO/GEO/BARY/LSRK/LSRD/GALACTO/REST).\n\n\n\n\n\n","title":"MeasurementSets.MFrequency"},{"category":"type","location":"api/#MeasurementSets.MRadialVelocity","page":"API reference","text":"MRadialVelocity{R}(mps)\n\nA radial velocity in m/s, referenced to velocity frame R (LSRK/LSRD/BARY/GEO/TOPO/GALACTO). measconvert converts between these frames (needs import SOFA and frame.direction).\n\n\n\n\n\n","title":"MeasurementSets.MRadialVelocity"},{"category":"type","location":"api/#MeasurementSets.MDoppler","page":"API reference","text":"MDoppler{C}(d)\n\nA Doppler shift as a dimensionless value in convention C (RADIO / OPTICAL / RATIO / BETA / GAMMA). measconvert(d, C2) changes convention; doppler / frequency / radialvelocity / restfrequency bridge to the other spectral measures (a rest frequency is needed where one is).\n\n\n\n\n\n","title":"MeasurementSets.MDoppler"},{"category":"type","location":"api/#MeasurementSets.MBaseline","page":"API reference","text":"MBaseline{R}(x, y, z)\n\nA baseline vector (antenna → antenna) in metres, in direction frame R (ITRF / J2000 / APP / AZEL / HADEC / GALACTIC / …). measconvert rotates it between frames (needs import SOFA), preserving its length — equivalent to converting the unit direction and rescaling.\n\n\n\n\n\n","title":"MeasurementSets.MBaseline"},{"category":"type","location":"api/#MeasurementSets.MuvW","page":"API reference","text":"MuvW{R}(u, v, w)\n\nA uvw baseline coordinate in metres, in direction frame R — the UVW column of an MS. Differs from an MBaseline by the rotation onto the frame whose w-axis points at the phase centre, so measconvert also needs frame.direction (the phase centre).\n\n\n\n\n\n","title":"MeasurementSets.MuvW"},{"category":"type","location":"api/#MeasurementSets.Ephemeris","page":"API reference","text":"Ephemeris\n\nAn opened solar-system ephemeris table (casacore MeasComet format). Construct with open_ephemeris or get one for a moving-target field with field_ephemeris; evaluate with ephemeris_direction / ephemeris_radvel / ephemeris_distance.\n\n\n\n\n\n","title":"MeasurementSets.Ephemeris"},{"category":"function","location":"api/#MeasurementSets.open_ephemeris","page":"API reference","text":"open_ephemeris(path) -> Ephemeris\n\nOpen a casacore ephemeris (MeasComet) table at path.\n\n\n\n\n\n","title":"MeasurementSets.open_ephemeris"},{"category":"function","location":"api/#MeasurementSets.field_ephemeris","page":"API reference","text":"field_ephemeris(fld::Table, field_id) -> Ephemeris | nothing\n\nIf FIELD row field_id (0-based) is a moving target — a non-negative EPHEMERIS_ID with a matching EPHEM<id>_*.tab in the FIELD subtable directory — return its opened Ephemeris, else nothing.\n\n\n\n\n\n","title":"MeasurementSets.field_ephemeris"},{"category":"function","location":"api/#MeasurementSets.ephemeris_direction","page":"API reference","text":"ephemeris_direction(e::Ephemeris, mjd) -> MDirection\n\nThe target direction at mjd (MJD, ideally TDB — a coarsely sampled table makes the UTC↔TDB difference negligible), in the table's own posrefsys frame.  Linearly interpolates the Cartesian (ρ, RA, Dec) vector between the two bracketing rows.\n\n\n\n\n\n","title":"MeasurementSets.ephemeris_direction"},{"category":"function","location":"api/#MeasurementSets.ephemeris_radvel","page":"API reference","text":"ephemeris_radvel(e::Ephemeris, mjd) -> Float64\n\nThe target radial velocity at mjd, m/s (linear interpolation of the RadVel column, AU/d → m/s).\n\n\n\n\n\n","title":"MeasurementSets.ephemeris_radvel"},{"category":"function","location":"api/#MeasurementSets.ephemeris_distance","page":"API reference","text":"ephemeris_distance(e::Ephemeris, mjd) -> Float64\n\nThe geocentric distance to the target at mjd, metres.\n\n\n\n\n\n","title":"MeasurementSets.ephemeris_distance"},{"category":"function","location":"api/#MeasurementSets.doppler","page":"API reference","text":"doppler(f::MFrequency, restfreq) -> MDoppler{BETA}\ndoppler(v::MRadialVelocity)      -> MDoppler{BETA}\n\nThe Doppler shift of f relative to restfreq (a frequency in Hz or an MFrequency), or of a radial velocity (β = v/c).  Result in the BETA (true v/c) convention – measconvert it to RADIO / OPTICAL / etc. as needed.\n\n\n\n\n\n","title":"MeasurementSets.doppler"},{"category":"function","location":"api/#MeasurementSets.frequency","page":"API reference","text":"frequency(d::MDoppler, restfreq)         -> MFrequency{LSRK}\nfrequency(v::MRadialVelocity, restfreq)  -> MFrequency{LSRK}\n\nThe frequency √((1−β)/(1+β)) · ν₀ implied by a Doppler shift (casacore fromDoppler) or a radial velocity v and a line rest frequency restfreq.  The result frame defaults to LSRK.\n\n\n\n\n\n","title":"MeasurementSets.frequency"},{"category":"function","location":"api/#MeasurementSets.radialvelocity","page":"API reference","text":"radialvelocity(d::MDoppler)              -> MRadialVelocity{LSRK}\nradialvelocity(f::MFrequency, restfreq)  -> MRadialVelocity{LSRK}\n\nThe true radial velocity of a Doppler shift (c·β, casacore fromDoppler), or of a frequency f relative to restfreq (= radialvelocity(doppler(f, restfreq))).  The two-argument form broadcasts — radialvelocity.(measure(spw, \"CHAN_FREQ\"), ν₀) is a velocity axis.  The result frame defaults to LSRK.\n\n\n\n\n\n","title":"MeasurementSets.radialvelocity"},{"category":"function","location":"api/#MeasurementSets.restfrequency","page":"API reference","text":"restfrequency(f::MFrequency, d::MDoppler) -> MFrequency{REST}\n\nThe rest frequency ν / √((1−β)/(1+β)) given an observed frequency and its Doppler shift (casacore toRest).\n\n\n\n\n\n","title":"MeasurementSets.restfrequency"},{"category":"function","location":"api/#MeasurementSets.shiftfreq","page":"API reference","text":"shiftfreq(d::MDoppler, ν) -> same shape as ν\n\nMultiply frequency(ies) ν by the Doppler factor √((1−β)/(1+β)) (β from d in the BETA convention) — the vectorised form of casacore MDoppler::shiftFrequency.  ν is a frequency in Hz, an MFrequency (the frame label is kept), or a vector of either (the factor is computed once).\n\nUnlike casacore, a non-BETA d is converted to BETA first.\n\n\n\n\n\n","title":"MeasurementSets.shiftfreq"},{"category":"function","location":"api/#MeasurementSets.reftype","page":"API reference","text":"reftype(m::Measure) -> Type{<:RefFrame}\n\nThe reference-frame type parameter of a measure value (reftype(MEpoch{UTC}(0)) === UTC).\n\n\n\n\n\n","title":"MeasurementSets.reftype"},{"category":"type","location":"api/#MeasurementSets.RefFrame","page":"API reference","text":"RefFrame\n\nAbstract supertype of every measure reference frame.  Concrete singleton subtypes, used as the type parameter of a measure value (MEpoch{UTC}, MDirection{J2000}) and as the target of measconvert:\n\ngroup frames\nepoch (time scale) UTC TAI TT TDB UT1\ndirection J2000 ICRS B1950 APP GALACTIC ECLIPTIC HADEC AZEL AZELGEO\nearth-fixed ITRF WGS84 TOPO\nfrequency / radial velocity REST LSRK LSRD BARY GEO GALACTO\n\nAn unrecognised casacore frame name is carried as OtherRef{:Name} (parsed, not convertible).\n\n\n\n\n\n","title":"MeasurementSets.RefFrame"},{"category":"type","location":"api/#MeasurementSets.DopplerType","page":"API reference","text":"A Doppler-shift convention (RADIO / OPTICAL / RATIO / BETA / GAMMA).\n\n\n\n\n\n","title":"MeasurementSets.DopplerType"},{"category":"type","location":"api/#MeasurementSets.UTC","page":"API reference","text":"Coordinated Universal Time (epoch scale).\n\n\n\n\n\n","title":"MeasurementSets.UTC"},{"category":"type","location":"api/#MeasurementSets.TAI","page":"API reference","text":"International Atomic Time (epoch scale).\n\n\n\n\n\n","title":"MeasurementSets.TAI"},{"category":"type","location":"api/#MeasurementSets.TT","page":"API reference","text":"Terrestrial Time (epoch scale).\n\n\n\n\n\n","title":"MeasurementSets.TT"},{"category":"type","location":"api/#MeasurementSets.TDB","page":"API reference","text":"Barycentric Dynamical Time (epoch scale).\n\n\n\n\n\n","title":"MeasurementSets.TDB"},{"category":"type","location":"api/#MeasurementSets.UT1","page":"API reference","text":"Universal Time UT1 (epoch scale; needs ΔUT1).\n\n\n\n\n\n","title":"MeasurementSets.UT1"},{"category":"type","location":"api/#MeasurementSets.J2000","page":"API reference","text":"Mean equator and equinox of J2000.0 (direction; treated as ICRS).\n\n\n\n\n\n","title":"MeasurementSets.J2000"},{"category":"type","location":"api/#MeasurementSets.ICRS","page":"API reference","text":"International Celestial Reference System (direction).\n\n\n\n\n\n","title":"MeasurementSets.ICRS"},{"category":"type","location":"api/#MeasurementSets.B1950","page":"API reference","text":"FK4 B1950.0 (direction).\n\n\n\n\n\n","title":"MeasurementSets.B1950"},{"category":"type","location":"api/#MeasurementSets.APP","page":"API reference","text":"Geocentric apparent place, equinox of date (direction).\n\n\n\n\n\n","title":"MeasurementSets.APP"},{"category":"type","location":"api/#MeasurementSets.GALACTIC","page":"API reference","text":"Galactic coordinates (direction).\n\n\n\n\n\n","title":"MeasurementSets.GALACTIC"},{"category":"type","location":"api/#MeasurementSets.ECLIPTIC","page":"API reference","text":"J2000 ecliptic coordinates (direction).\n\n\n\n\n\n","title":"MeasurementSets.ECLIPTIC"},{"category":"type","location":"api/#MeasurementSets.HADEC","page":"API reference","text":"Topocentric hour angle / declination (direction).\n\n\n\n\n\n","title":"MeasurementSets.HADEC"},{"category":"type","location":"api/#MeasurementSets.AZEL","page":"API reference","text":"Azimuth / elevation about the geocentric vertical, N=0 E=90 (direction).\n\n\n\n\n\n","title":"MeasurementSets.AZEL"},{"category":"type","location":"api/#MeasurementSets.AZELGEO","page":"API reference","text":"Azimuth / elevation about the geodetic vertical (direction).\n\n\n\n\n\n","title":"MeasurementSets.AZELGEO"},{"category":"type","location":"api/#MeasurementSets.ITRF","page":"API reference","text":"International Terrestrial Reference Frame (position / direction).\n\n\n\n\n\n","title":"MeasurementSets.ITRF"},{"category":"type","location":"api/#MeasurementSets.WGS84","page":"API reference","text":"WGS84 geodetic datum (position).\n\n\n\n\n\n","title":"MeasurementSets.WGS84"},{"category":"type","location":"api/#MeasurementSets.TOPO","page":"API reference","text":"Topocentric (frequency / direction).\n\n\n\n\n\n","title":"MeasurementSets.TOPO"},{"category":"type","location":"api/#MeasurementSets.REST","page":"API reference","text":"Source rest frame (frequency).\n\n\n\n\n\n","title":"MeasurementSets.REST"},{"category":"type","location":"api/#MeasurementSets.LSRK","page":"API reference","text":"Kinematic Local Standard of Rest (frequency / radial velocity).\n\n\n\n\n\n","title":"MeasurementSets.LSRK"},{"category":"type","location":"api/#MeasurementSets.LSRD","page":"API reference","text":"Dynamical Local Standard of Rest (frequency / radial velocity).\n\n\n\n\n\n","title":"MeasurementSets.LSRD"},{"category":"type","location":"api/#MeasurementSets.BARY","page":"API reference","text":"Solar-system barycentre (frequency / radial velocity).\n\n\n\n\n\n","title":"MeasurementSets.BARY"},{"category":"type","location":"api/#MeasurementSets.GEO","page":"API reference","text":"Geocentric (frequency / radial velocity).\n\n\n\n\n\n","title":"MeasurementSets.GEO"},{"category":"type","location":"api/#MeasurementSets.GALACTO","page":"API reference","text":"Galactocentric (frequency / radial velocity).\n\n\n\n\n\n","title":"MeasurementSets.GALACTO"},{"category":"type","location":"api/#MeasurementSets.LGROUP","page":"API reference","text":"Local Group barycentre, ~308 km/s (frequency / radial velocity).\n\n\n\n\n\n","title":"MeasurementSets.LGROUP"},{"category":"type","location":"api/#MeasurementSets.CMB","page":"API reference","text":"Cosmic microwave background rest frame, ~369.5 km/s (frequency / radial velocity).\n\n\n\n\n\n","title":"MeasurementSets.CMB"},{"category":"type","location":"api/#MeasurementSets.MERCURY","page":"API reference","text":"The planet Mercury as a direction (geocentric apparent place).\n\n\n\n\n\n","title":"MeasurementSets.MERCURY"},{"category":"type","location":"api/#MeasurementSets.VENUS","page":"API reference","text":"The planet Venus as a direction (geocentric apparent place).\n\n\n\n\n\n","title":"MeasurementSets.VENUS"},{"category":"type","location":"api/#MeasurementSets.MARS","page":"API reference","text":"The planet Mars as a direction (geocentric apparent place).\n\n\n\n\n\n","title":"MeasurementSets.MARS"},{"category":"type","location":"api/#MeasurementSets.JUPITER","page":"API reference","text":"The planet Jupiter as a direction (geocentric apparent place).\n\n\n\n\n\n","title":"MeasurementSets.JUPITER"},{"category":"type","location":"api/#MeasurementSets.SATURN","page":"API reference","text":"The planet Saturn as a direction (geocentric apparent place).\n\n\n\n\n\n","title":"MeasurementSets.SATURN"},{"category":"type","location":"api/#MeasurementSets.URANUS","page":"API reference","text":"The planet Uranus as a direction (geocentric apparent place).\n\n\n\n\n\n","title":"MeasurementSets.URANUS"},{"category":"type","location":"api/#MeasurementSets.NEPTUNE","page":"API reference","text":"The planet Neptune as a direction (geocentric apparent place).\n\n\n\n\n\n","title":"MeasurementSets.NEPTUNE"},{"category":"type","location":"api/#MeasurementSets.SUN","page":"API reference","text":"The Sun as a direction (geocentric apparent place).\n\n\n\n\n\n","title":"MeasurementSets.SUN"},{"category":"type","location":"api/#MeasurementSets.MOON","page":"API reference","text":"The Moon as a direction (geocentric apparent place; topocentric when frame.position is set).\n\n\n\n\n\n","title":"MeasurementSets.MOON"},{"category":"type","location":"api/#MeasurementSets.RADIO","page":"API reference","text":"Radio velocity cΔν/ν₀ (D = 1 − ν/ν₀).\n\n\n\n\n\n","title":"MeasurementSets.RADIO"},{"category":"type","location":"api/#MeasurementSets.OPTICAL","page":"API reference","text":"Optical velocity / redshift z = cΔλ/λ₀ (D = ν₀/ν − 1).\n\n\n\n\n\n","title":"MeasurementSets.OPTICAL"},{"category":"type","location":"api/#MeasurementSets.RATIO","page":"API reference","text":"The frequency ratio ν/ν₀ itself.\n\n\n\n\n\n","title":"MeasurementSets.RATIO"},{"category":"type","location":"api/#MeasurementSets.BETA","page":"API reference","text":"True relativistic velocity v/c (D = (1−F²)/(1+F²), F = ν/ν₀).\n\n\n\n\n\n","title":"MeasurementSets.BETA"},{"category":"type","location":"api/#MeasurementSets.GAMMA","page":"API reference","text":"The Lorentz factor γ (D = (1+F²)/(2F)).\n\n\n\n\n\n","title":"MeasurementSets.GAMMA"},{"category":"type","location":"api/#MeasurementSets.Z","page":"API reference","text":"Alias for OPTICAL — casacore's Z (redshift) spelling.\n\n\n\n\n\n","title":"MeasurementSets.Z"},{"category":"type","location":"api/#MeasurementSets.RELATIVISTIC","page":"API reference","text":"Alias for BETA — casacore's RELATIVISTIC spelling.\n\n\n\n\n\n","title":"MeasurementSets.RELATIVISTIC"},{"category":"function","location":"api/#MeasurementSets.columndesc","page":"API reference","text":"columndesc(t, name) -> ColumnDesc\n\nThe ColumnDesc (type, cell shape, keywords, storage-manager binding) for column name of table t. Throws KeyError if there is no such column.\n\n\n\n\n\n","title":"MeasurementSets.columndesc"},{"category":"type","location":"api/#MeasurementSets.ColumnDesc","page":"API reference","text":"ColumnDesc\n\nDescription of one table column: its name, element type (a CasaType), cell shape (a CellShape — () scalar, a Dims tuple, VariableShape, or VariableDims), comment, per-column keywords (a Record, holding units / MEASINFO / engine config), and the storage-manager manager type + group (instance) it is bound to. Obtained from columndesc; the fields are read-only.\n\n\n\n\n\n","title":"MeasurementSets.ColumnDesc"},{"category":"type","location":"api/#MeasurementSets.TableDesc","page":"API reference","text":"TableDesc\n\nA table's schema: its name, format version, the public and private keyword sets (each a Record), and the ordered list of ColumnDesc columns. Reachable as t.desc on a Table; most code uses columnnames / columndesc / keywords instead.\n\n\n\n\n\n","title":"MeasurementSets.TableDesc"},{"category":"function","location":"api/#MeasurementSets.keywords","page":"API reference","text":"keywords(t) -> Record\n\nThe table-level keyword set of t as a Record (MS_VERSION, MEASURE_REFERENCE, subtable references, …). Per-column keywords live on columndesc(t, name).keywords.\n\n\n\n\n\n","title":"MeasurementSets.keywords"},{"category":"function","location":"api/#MeasurementSets.subtables","page":"API reference","text":"subtables(t::Table) -> Vector{Pair{String,String}}\n\nKeyword name => subtable directory path, for every TpTable keyword.\n\n\n\n\n\n","title":"MeasurementSets.subtables"},{"category":"type","location":"api/#MeasurementSets.Record","page":"API reference","text":"An ordered casacore (Table)Record: field name -> value, with the on-disk types.\n\n\n\n\n\n","title":"MeasurementSets.Record"},{"category":"type","location":"api/#MeasurementSets.CasaType","page":"API reference","text":"CasaType\n\nEnum mirroring casacore's DataType — the element type of a column or keyword value (TpBool, TpInt, TpFloat, TpComplex, TpString, the TpArray* variants, TpInt64, …). It is the type field of a ColumnDesc; juliatype(t) gives the corresponding Julia type.\n\n\n\n\n\n","title":"MeasurementSets.CasaType"},{"category":"type","location":"api/#MeasurementSets.CellShape","page":"API reference","text":"Cell-shape descriptor for a column:\n\n()             — scalar\na Dims tuple   — array with a fixed cell shape\nVariableShape  — array, fixed dimensionality, per-row-variable shape\nVariableDims   — array whose dimensionality itself varies per row\n\n\n\n\n\n","title":"MeasurementSets.CellShape"},{"category":"type","location":"api/#MeasurementSets.VariableShape","page":"API reference","text":"An array column with a fixed number of axes but a per-row-variable cell shape (casacore ndim > 0 with no declared shape, e.g. DATA, FLAG_CATEGORY).\n\n\n\n\n\n","title":"MeasurementSets.VariableShape"},{"category":"type","location":"api/#MeasurementSets.VariableDims","page":"API reference","text":"An array column whose dimensionality itself varies per row (casacore ndim == -1, e.g. ASSOC_SPW_ID).\n\n\n\n\n\n","title":"MeasurementSets.VariableDims"},{"category":"function","location":"api/#MeasurementSets.isarray","page":"API reference","text":"Whether column c holds arrays (shape other than ()).\n\n\n\n\n\n","title":"MeasurementSets.isarray"},{"category":"constant","location":"api/#MeasurementSets.SCHEMAVER2","page":"API reference","text":"SCHEMAVER2 :: Dict{String,StdTable}\n\nThe MS version 2 standard schema (NRAO Memo 229 §5), keyed by table name (\"MAIN\", \"ANTENNA\", \"SPECTRAL_WINDOW\", …). Use stdtable / stdcolumns to query it and validate to check a table against it.\n\n\n\n\n\n","title":"MeasurementSets.SCHEMAVER2"},{"category":"type","location":"api/#MeasurementSets.StdTable","page":"API reference","text":"StdTable\n\nThe standard-schema definition of one MS table: its name, columns (StdColumn), required keywords, and (MAIN only) required subtables. Looked up in SCHEMAVER2 / via stdtable(name).\n\n\n\n\n\n","title":"MeasurementSets.StdTable"},{"category":"type","location":"api/#MeasurementSets.StdColumn","page":"API reference","text":"StdColumn\n\nOne column of the MS v2 standard schema (NRAO Memo 229 §5): its name, type (CasaType), cell shape, unit, whether it is required, and a comment. Reachable via stdcolumns / stdtable(name).columns.\n\n\n\n\n\n","title":"MeasurementSets.StdColumn"},{"category":"function","location":"api/#MeasurementSets.stdtable","page":"API reference","text":"stdtable(name) -> StdTable\n\nThe standard MS v2 definition for table name (\"MAIN\", \"ANTENNA\", …).\n\n\n\n\n\n","title":"MeasurementSets.stdtable"},{"category":"function","location":"api/#MeasurementSets.stdcolumns","page":"API reference","text":"stdcolumns(name) -> Vector{StdColumn}\n\nThe standard-schema columns of MS v2 table name — shorthand for stdtable(name).columns.\n\n\n\n\n\n","title":"MeasurementSets.stdcolumns"},{"category":"function","location":"api/#MeasurementSets.validate","page":"API reference","text":"validate(t::AbstractTable; table=\"MAIN\") -> Vector{String}\n\nCheck t against the standard schema for table.  Returns a list of issues (missing required column, element-type mismatch, missing required subtable, unexpected keyword value); empty means conformant.  Never throws.\n\n\n\n\n\nvalidate(ms::MeasurementSet) -> Vector{String}\n\nValidate MAIN and every present standard subtable.\n\n\n\n\n\n","title":"MeasurementSets.validate"},{"category":"function","location":"api/#MeasurementSets.write_table","page":"API reference","text":"write_table(dir, name, columns; nrow, endian=:little,\n           tsm, tcm, tcell, ism, engines, virtualtaql, dysco, dysco_spec,\n           measures, units, storage=:sepfile, blocksize=DEFAULT_MF_BLOCKSIZE,\n           type=\"\", subtype=\"\", readme=\"\")\n\nWrite a CTDS table at dir.  columns is an iterable of name => vector pairs (or a Tables columns source).  Column metadata (units, comments, exact class names) is taken from SCHEMAVER2[name] when available. dysco/dysco_spec compress one or more columns with DyscoStMan – see _write_table_core for the exact shape.  virtualtaql maps a column name to a TaQL-lite CALC expression (a VirtualTaQLColumn – the passed values for that column are ignored, only the expression is stored). storage/blocksize pack every StandardStMan/IncrementalStMan/TiledStMan private file into one table.mf/table.mfh5 – see _write_table_core.\n\nA column whose Julia element type is a Unitful quantity or a Measure (MEpoch{UTC}, MDirection{J2000}, MPosition{ITRF}, MFrequency, MRadialVelocity) is stored as plain numbers with its QuantumUnits (and, for a Measure, MEASINFO) column keyword stamped automatically – so write_table -> readtable -> qcolumn / measure round-trips. measures = Dict(col => (; kind, ref[, varrefcol, units])) and units = Dict(col => \"Hz\" | [\"rad\",\"rad\"]) stamp those keywords explicitly and override the auto-detection for that column.\n\n\n\n\n\n","title":"MeasurementSets.write_table"},{"category":"function","location":"api/#MeasurementSets.write_ms","page":"API reference","text":"write_ms(dir, ms::MeasurementSet; rows=Colon(), subtables=Colon(),\n        storage=:sepfile, blocksize=DEFAULT_MF_BLOCKSIZE)\n\nWrite ms to a new MeasurementSet directory dir: MAIN restricted to rows, and every subtable in full (or only those named in subtables, a collection of keyword names).  A column the reader cannot decode is skipped with a warning.  storage/blocksize, if not :sepfile, pack every table written (MAIN and each subtable) into its own table.mf/ table.mfh5 – matching what a real casacore MS created under a global StorageOption would look like (one container per table, not one shared container for the whole MS tree).\n\n\n\n\n\n","title":"MeasurementSets.write_ms"},{"category":"function","location":"api/#MeasurementSets.copyms","page":"API reference","text":"copyms(src, dst; rows=Colon(), subtables=Colon(),\n      storage=:sepfile, blocksize=DEFAULT_MF_BLOCKSIZE)\n\nCopy the MeasurementSet at src to a new directory dst (MAIN rows optionally sliced; subtables optionally restricted to a set of names). storage/blocksize – see write_ms.\n\n\n\n\n\n","title":"MeasurementSets.copyms"},{"category":"function","location":"api/#MeasurementSets.copytable","page":"API reference","text":"copytable(dst, t; rows=Colon(), name=\"TABLE\",\n         storage=:sepfile, blocksize=DEFAULT_MF_BLOCKSIZE) -> dst\n\nDeep-copy t into a fresh plain table at dst.  For a Table / RefTable / ConcatTable each column's storage-manager / virtual-engine kind is preserved (casacore's GIVING ... AS PLAIN); for a GroupedTable the columns are materialised (name sets the table's schema name).  rows selects/reorders rows (1-based into t); storage/blocksize pack the destination into one table.mf / table.mfh5 – see _write_table_core.  This is \"SELECT ... INTO\" for any query result.\n\n\n\n\n\n","title":"MeasurementSets.copytable"},{"category":"function","location":"api/#MeasurementSets.create_ms","page":"API reference","text":"create_ms(dir; nrow=10, nchan=4, ncorr=2, nant=3, nrec=2,\n         storage=:sepfile, blocksize=DEFAULT_MF_BLOCKSIZE)\n\nSynthesise a minimal, validate-clean MeasurementSet v2 at dir (zero / default-valued data).  MAIN's DATA/FLAG go through TiledShapeStMan; scalars and fixed-shape arrays through StandardStMan direct cells; variable-shape array columns (CHAN_FREQ, CORR_TYPE, POLARIZATION_TYPE, …) through StandardStMan indirect arrays.  storage/blocksize, if not :sepfile, pack every table (MAIN and each subtable) into its own table.mf/table.mfh5 – see write_ms.\n\n\n\n\n\n","title":"MeasurementSets.create_ms"},{"category":"function","location":"api/#MeasurementSets.reference_copy","page":"API reference","text":"reference_copy(dst, src; writable=String[]) -> dst\n\nCreate a new table at dst whose columns are ForwardColumnEngine references to src (a path or a plain Table) – each read forwards to the same-named column in src, and no data is copied.  Columns named in writable are real independent copies instead.  Mirrors casacore's MSTableImpl::referenceCopy.\n\n\n\n\n\n","title":"MeasurementSets.reference_copy"},{"category":"function","location":"api/#MeasurementSets.write_reftable","page":"API reference","text":"write_reftable(dir, parent, rows; select) -> dir\n\nPersist a row-number reference to parent (a Table, RefTable, or ConcatTable) at dir, in casacore's RefTable format — openable by casa / python-casacore.  rows are 1-based row indices into parent (any order, repeats allowed).  select is output_name => parent_name pairs in output order (default: every column of parent, unrenamed).\n\n\n\n\n\n","title":"MeasurementSets.write_reftable"},{"category":"function","location":"api/#MeasurementSets.write_concattable","page":"API reference","text":"write_concattable(dir, parts; subtabnames=String[]) -> dir\n\nPersist a virtual row-wise concatenation of parts (same-schema tables) at dir.  subtabnames lists keyword subtables to also concatenate (rarely used; default none).\n\n\n\n\n\n","title":"MeasurementSets.write_concattable"},{"category":"function","location":"api/#MeasurementSets.edit","page":"API reference","text":"edit(path) -> EditTable\nedit(f, path)            # runs `f(t)`, then `flush(t)`\n\nOpen the CTDS table at path for update.\n\n\n\n\n\n","title":"MeasurementSets.edit"},{"category":"function","location":"api/#MeasurementSets.addrows!","page":"API reference","text":"addrows!(t, n)\n\nAppend n rows.  Appended cells read back as zeros / \"\" (or a VariableShape cell as the previous row's shape) until written.\n\n\n\n\n\n","title":"MeasurementSets.addrows!"},{"category":"function","location":"api/#MeasurementSets.removerows!","page":"API reference","text":"removerows!(t, rows)\n\nDelete rows (1-based indices into the current row set).  Forces the regen persist path on flush.\n\n\n\n\n\n","title":"MeasurementSets.removerows!"},{"category":"function","location":"api/#MeasurementSets.addcolumn!","page":"API reference","text":"addcolumn!(t, name; kind=:ssm)\naddcolumn!(t, name, data; kind=:ssm, type=nothing, shape=nothing)\n\nAdd a column.  With no data the column is taken from the standard MS v2 schema and filled with default cells; otherwise data (length length(t))) supplies the values and its element type / shape are inferred unless type / shape are given.  kind binds the column to a new/shared StandardStMan (:ssm), IncrementalStMan (:ism) or a per-column TiledShapeStMan (:tsm).\n\n\n\n\n\n","title":"MeasurementSets.addcolumn!"},{"category":"function","location":"api/#MeasurementSets.removecolumn!","page":"API reference","text":"removecolumn!(t, name)\n\nDrop a column.  If it takes a whole storage-manager instance with it, that instance's files are deleted on flush.\n\n\n\n\n\n","title":"MeasurementSets.removecolumn!"},{"category":"function","location":"api/#MeasurementSets.setcell!","page":"API reference","text":"setcell!(t, name, i, v) -> t\n\nSet cell i of column name in the edit session t to v — the verb behind t[name][i] = v.\n\n\n\n\n\n","title":"MeasurementSets.setcell!"},{"category":"function","location":"api/#MeasurementSets.setcolumn!","page":"API reference","text":"setcolumn!(t, name, vals) -> t\n\nReplace the whole of column name in the edit session t with vals (length must equal the current row count) — the verb behind t[name][:] = vals.\n\n\n\n\n\n","title":"MeasurementSets.setcolumn!"},{"category":"function","location":"api/#MeasurementSets.resync","page":"API reference","text":"resync(t) -> Table / MeasurementSet\n\nRe-open the table if is_stale; otherwise return t unchanged. The returned object replaces t – keep it and drop the old one.\n\n\n\n\n\n","title":"MeasurementSets.resync"},{"category":"function","location":"api/#MeasurementSets.is_stale","page":"API reference","text":"is_stale(t) -> Bool\n\nWhether the table.lock sync blob has been updated (by another process, or by our own edit) since t was opened.\n\n\n\n\n\n","title":"MeasurementSets.is_stale"},{"category":"function","location":"api/#MeasurementSets.is_multiused","page":"API reference","text":"is_multiused(dir) -> Bool\n\nWhether another process currently has the table open (holds the byte-1 \"in use\" read lock).  Uses a fresh probe fd because F_GETLK never reports a conflict with the calling process.\n\n\n\n\n\n","title":"MeasurementSets.is_multiused"},{"category":"function","location":"api/#MeasurementSets.query","page":"API reference","text":"query(t::AbstractTable, wherestr::AbstractString;\n     select=[n=>n for n in columnnames(t)]) -> RefTable | GroupedTable\n\nRow-filter t with a small TaQL-like WHERE expression:\n\ncomparisons ==/=, !=/<>, <, <=, >, >=;\nAND/&&, OR/||, NOT/!, parentheses;\narithmetic + - * / % // ** and unary - on numeric operands (A + 1 > B, ANTENNA1 % 4 == 0, 2 ** N) — Julia numeric semantics (/ yields a float, // truncates, % is rem);\ncol IN [v1, v2, ...] / col NOT IN [...];\ncol LIKE 'pat' / ILIKE (case-insensitive) / NOT LIKE — SQL glob (% = any run, _ = one char); and TaQL's col ~ p/glob/, ~ m/regex/, ~ f/regex/ (and !~), delimiters / % @, optional trailing i;\nfunctions NAME(args...) (case-insensitive, TaQL's aliases) — scalar math (abs, sqrt, exp, log/ln, log10, trig, floor/ceil/ round, sign, int, pow, fmod), complex parts (real, imag, arg/phase, conj, norm), array-cell reductions (mean/avg, sum, product, median, variance, stddev, rms, min/max, any, all, ntrue/nfalse, nelements/count, ndim), string ops (strlength/len, upper/lower, trim/ltrim/rtrim), isnan/isinf/isfinite/nonfinite, masked arrays (marray(d, m), arraydata, arraymask; V[boolexpr] yields a masked array whose reductions skip the excluded elements), iif(cond, a, b), rownumber() (1-based), pi, e;\nan optional trailing ORDER BY col [ASC|DESC], ... (bare columns).\n\nColumn names are case-sensitive and must name a column of t; keywords and function names are case-insensitive. Only the columns actually referenced (by the WHERE expression or an ORDER BY key) are read.\n\nselect is \"out\" => rhs pairs. When every rhs is a bare column name (or Symbol) the result is a lazy RefTable (no data copied; persist it with write_reftable(dst, result)). When any rhs is a computed expression (\"X * 2\", \"sqrt(abs(V))\", \"iif(K==0, 1, 0)\" — same grammar as WHERE, aggregates excepted) the result is an in-memory GroupedTable with those columns evaluated per matched row. A computed column that is a masked array (\"V[FLAG]\") persists as its plain data. A (\"val\", \"mask\") => \"expr\" pair entry (TaQL's expr AS (v, m)) instead emits two columns — the data and the mask — so [(\"D\", \"F\") => \"marray(DATA, FLAG)\"] reads a column together with its mask column.\n\nA bare \"ORDER BY ...\" (no WHERE) matches every row, sorted.\n\nmscal.* derived-MS functions (Phase 77, needs import SOFA and an MS MAIN table with ANTENNA / FIELD subtables): mscal.ha1() / ha2() / ha() (hour angle, rad), mscal.hadec1() ([ha, dec]), mscal.azel1() ([az, el]), mscal.az1() / el1() (scalar), mscal.pa1() (parallactic angle), mscal.last1() (local apparent sidereal time, rad), mscal.itrf() ([lon, lat] of PHASE_DIR in ITRF), mscal.uvw_j2000() ([u, v, w] m — the UVW column in J2000), mscal.delay() (geometric delay, s). The 1 / 2 suffix picks ANTENNA1 / ANTENNA2; no suffix uses antenna 0 (array-centre fallback). The direction functions (ha / hadec / azel / az / el / pa / itrf / delay) take an optional direction argument instead of FIELD.PHASE_DIR — a body name (mscal.el1('SUN')), a FIELD direction column (mscal.az1('DELAY_DIR')), a [ra, dec] J2000 pair in radians (mscal.hadec1([2.0, 0.5])), or a sexagesimal 'RA, DEC' string (mscal.el1('10h42m31, 45d51m16')).\n\nmscal.stokes(col [, 'types'] [, rescale]) (Phase 78) converts a DATA / FLAG / WEIGHT array cell between correlation bases. types (default 'IQUV') is an alias (IQUV / CIRC / LIN) or a comma-list ('I', 'I,V', 'XX,YY'); the input basis comes from POLARIZATION.CORR_TYPE row 1. The result is a (nOut, nchan) matrix.\n\nmscal.<sel>('spec') (Phase 80) — MSSelection-lite row selection, returning a per-row Bool. <sel> ∈ baseline / field / spw / scan / state / array / obs. spec is a comma-list of terms (N, N~M, >N / <N, an exact / glob / /regex/ name match against the type's NAME column); a !-term is subtracted. mscal.baseline also takes L & R / L && R (baseline between two antenna sets; & drops autocorrelations) and a whole-spec !. mscal.time('t0~t1') selects a TIME range (endpoints are ISO / YYYY/MM/DD[/HH:MM:SS] datetimes or a bare MJD-days number); mscal.uvdist('a~b[unit]') selects a 2-D uv-distance range, unit ∈ m (default) / km / lambda / klambda / mlambda (wavelength units scale per row by SPECTRAL_WINDOW.REF_FREQUENCY). mscal.corr('RR,LL') selects rows whose polarization setup contains any of the named correlations (Stokes names or integer codes); mscal.feed('0 & 1') is the mscal.baseline form on FEED1 / FEED2.\n\nmscal.spw('0:5~20') — the spw selection takes an optional :chanlist (a ;-list of a~b, a~b^step channel-index ranges, single indices, or f1~f2GHz / <f / >f CHAN_FREQ ranges); a channelled spw selects the row only if at least one of the row's channels is selected. mscal.chan('0:5~20') returns a per-row BitVector (the spw's channel count) — the selected-channel mask, for use with any(...) / count(...) / a masked array.\n\nDeliberately a subset of real TaQL's grammar, not a look-alike: no boolean-mask array subscripts. Supported: 1-based array element/slice indexing (DATA[1,1], V[1:4,1], UVW[-1], V[end-2:end,1]), array literals [a, b, ...], BETWEEN / NOT BETWEEN (inclusive), bitwise & | ^ ~ (^ is xor – use ** for exponentiation), ~= / !~= approximate equality (casacore's near, relative tolerance 1e-5), scientific-notation number literals (1.4e9), quantity literals (1.4GHz, 10arcsec, 30deg – compared against a column carrying a QuantumUnits keyword; needs the Unitful extension), sexagesimal literals (10h30m, 45d51m16s, 12h, 45d – an h-prefix is an hour angle ×15, a d-prefix is degrees; the value is radians), and date/time + angle functions: datetime/mjd/mjdtodate/date/time (all MJD-day Float64), year/month/day/week/weekday, cdate/ctime/cmonth/cdow/ ctod, hms/dms (radians → sexagesimal string), angle('10h30m') (sexagesimal string → radians), normangle, angdist/angdistx (4 scalar radians or two [lon, lat] arrays).\n\n\n\n\n\nquery(f::Function, t::AbstractTable; cols=nothing, orderby=nothing,\n     select=[n=>n for n in columnnames(t)]) -> RefTable | GroupedTable\n\nRow-filter t with a Julia predicate f(row) -> Bool (do-block friendly: query(t; cols=[...]) do row ... end). row is a Tables.AbstractRow supporting row.COLNAME property access. cols restricts which columns are actually read (default: every column — f is an opaque closure, so unlike the string-based query its column use can't be inferred; pass cols explicitly on a wide table to avoid materialising columns f never touches). orderby sorts the matched rows by one or more columns: each entry is a bare column name/Symbol (ascending) or a name => :asc/name => :desc pair. select — see the string-based query above.\n\n\n\n\n\nquery(gt::GroupedTable, wherestr; select=identity) -> GroupedTable\n\nFilter / project / sort an in-memory columnar result. wherestr is a TaQL-lite WHERE expression (+ optional trailing ORDER BY) over the result's column names; select is outname => source_name pairs.\n\n\n\n\n\nquery(f, gt::GroupedTable; cols=nothing, orderby=nothing, select=identity) -> GroupedTable\n\nClosure form: keep the rows for which f(row) -> Bool (row.OUTNAME property access). See the string form above.\n\n\n\n\n\n","title":"MeasurementSets.query"},{"category":"function","location":"api/#MeasurementSets.groupby","page":"API reference","text":"groupby(t, groupcols; select, cols=nothing, where=nothing, having=nothing, orderby=nothing) -> GroupedTable\n\nGroup the rows of t by groupcols (a column name / Symbol, or a vector of them; an empty vector = one group over the whole table) and compute one result row per group. t is any AbstractTable — a Table, a RefTable, or another groupby / join / query result, so the verbs chain.\n\nselect is outname => rhs pairs, where rhs is one of:\n\na string — a TaQL-lite expression that may use g-prefixed aggregate functions over the group: gcount() / gcount(x) (row count), gsum(x), gproduct(x), gmean(x) / gavg(x), gmedian(x), gmin(x), gmax(x), gvariance(x) / gsamplevariance(x), gstddev(x) / gsamplestddev(x), grms(x), gany(x), gall(x), gntrue(x), gnfalse(x), gfirst(x), glast(x) — plus the group-key columns and scalar expressions of them. An aggregate's argument is normally a scalar per row (wrap an array cell in mean(...) / sum(...)), OR a masked array (gmean(V[!FLAG]) pools every row's unmasked elements). The s-suffixed per-element variants (gsums, gproducts, gmeans / gavgs, gvariances / gsamplevariances, gstddevs / gsamplestddevs, grmss, gmins, gmaxs, ganys, galls, gntrues, gnfalses) reduce the group's array cells per position — over the rows where that cell is unmasked (all cells if unmasked), giving one array.\na Symbol — shorthand for a bare column name (:K ≡ \"K\").\na function g -> value — called with a GroupSlice (see the do-block form below); use for aggregates the g* set can't express.\n\nwhere pre-filters rows (a WHERE string, or a row -> Bool closure). having filters groups (a HAVING string over aggregates/keys, or a g -> Bool closure). cols restricts which columns are loaded onto a GroupSlice — only relevant when select/where/having use a closure (default: every column of t). orderby sorts the result rows by output column name(s): \"N\" (ascending) or \"N\" => :desc.\n\nrollup = true / cube = true / grouping_sets = [...] compute multiple SQL grouping sets and stack the result rows. rollup groups by each key prefix (keys, keys[1:end-1], …, ()); cube by every subset; grouping_sets by exactly the sets you list — each an iterable of key names, () for the grand total (grouping_sets = [(\"K1\",\"K2\"), (\"K1\",), ()]). At most one of the three. In a row where a key is aggregated away, that key column is missing — a bare :K / \"K\" select entry emits missing; a closure should build its key fields from g.keys ((; g.keys..., N = length(g))).\n\nGROUPING(K) — usable in a select or having string, and as g.grouping.K in a closure — is true when key K is rolled up in that row (SQL's GROUPING()), e.g. select = [\"label\" => \"iif(GROUPING(K2), 'ALL', K2)\", …] or having = \"GROUPING(K1) == 0\".\n\n(casacore parses ROLLUP/CUBE/GROUPING SETS but does not implement them, so this is plain SQL semantics with no real-TaQL cross-check.)\n\nReturns a GroupedTable.\n\n\n\n\n\ngroupby(f, t, groupcols; cols=nothing, where=nothing, having=nothing, orderby=nothing) -> GroupedTable\n\nClosure form (do-block friendly). f(g) receives a GroupSlice for each group and returns a NamedTuple — that group's output row. Every group must return the same field names; they become the result's columns, in that order.\n\ngroupby(t, [:ANTENNA1]; cols=[\"ANTENNA1\", \"DATA\"]) do g\n    (; ANT = first(g.ANTENNA1), N = length(g), AMP = mean(abs.(g.DATA)))\nend\n\ncols restricts which columns are loaded onto g (default: every column of t — f is opaque). where / having — a string or a predicate closure (row -> Bool / g -> Bool). orderby — see the string form. rollup / cube / grouping_sets — see the string form; a multi-set closure should use g.keys for its key fields ((; g.keys..., N = length(g))) so aggregated-away keys come back as missing, and may test g.grouping.K.\n\n\n\n\n\n","title":"MeasurementSets.groupby"},{"category":"type","location":"api/#MeasurementSets.GroupSlice","page":"API reference","text":"GroupSlice\n\nThe per-group value passed to a closure-form groupby (the do-block argument, and the argument of a select / where / having closure). g.COLNAME is a materialised Vector of that column's values for the group's rows; length(g) is the group size; propertynames(g) lists the loaded columns.\n\nOnly the columns named in cols= (or every column, when cols is omitted) are available — a closure's column use cannot be inferred. The names cols, rows, keys, level are struct fields / synthesised properties, so a column with one of those names is unreachable as g.<name> (a non-issue for MS column names).\n\ng.keys is a NamedTuple of the grouping-key values for this group (one scalar per key). g.level is the number of active grouping keys — always length(groupcols) for a plain groupby, but for a rollup=true subtotal level the trailing keys are inactive and their g.keys entries are missing. A rollup closure should build its key output fields from g.keys ((; g.keys..., N = length(g))).\n\n\n\n\n\n","title":"MeasurementSets.GroupSlice"},{"category":"function","location":"api/#MeasurementSets.update!","page":"API reference","text":"update!(target; set, where=nothing) -> Int\n\nChange column values in the CTDS table at target (a path or an open Table / subtable(ms, …)). set is \"COL\" => \"expr\" pairs — each expr a TaQL-lite expression over the row's columns, evaluated against the pre-update values (so set = [\"A\" => \"B\", \"B\" => \"A\"] swaps). The set key may also be an array-slice or boolean-mask target, \"COL[subscripts]\" => \"expr\" (TaQL's UPDATE … SET NAME[i,j] = … / NAME[maskexpr] = …) — 1-based, end-relative and range subscripts, or a single Bool-array subscript acting as a write mask — which writes only the addressed elements, leaving the rest untouched; a scalar RHS fills the region. col[slice][mask] and col[mask][slice] both work.\n\nA set key may also be a (datacol, maskcol) tuple (TaQL's UPDATE … SET (NAME, MASKNAME) = …). (\"D\", \"M\") => \"dexpr\" writes dexpr to D and, to M, the mask of dexpr when it is a masked array (SET (D, M) = V[goodcond] → M gets !goodcond), else a Bool array flagging where dexpr's result is non-finite. (\"D\", \"M\") => (\"dexpr\", \"mexpr\") writes mexpr (any Bool expression) to M instead. Either name may be a slice / mask target.\n\nwhere is a TaQL-lite WHERE string, a row -> Bool closure, or nothing (every row). Returns the number of rows changed.\n\n\n\n\n\n","title":"MeasurementSets.update!"},{"category":"function","location":"api/#MeasurementSets.taql","page":"API reference","text":"taql(target, command::AbstractString)\n\nRun one TaQL-lite write / select command against target (a path or an open Table):\n\nUPDATE [t] SET c1 = e1, c2 = e2 [WHERE cond]  → update!, returns Int\nDELETE [FROM t] [WHERE cond]                  → delete!, returns Int\nSELECT [*|col [AS a], …] [WHERE cond] (INTO|GIVING) 'path'  → copytable, returns the path\nSELECT … with no INTO/GIVING              → query, returns the result\nINSERT INTO t [(c1, c2)] VALUES (v1, v2), (…) [LIMIT n]  → insert!, returns Int\nINSERT [LIMIT n] INTO t SET c1 = v1, c2 = v2   → insert!, returns Int\n\nINSERT values must be constant expressions (no column references). Clause keywords (SET / WHERE / INTO / GIVING / FROM) are found by a case-insensitive split; a quoted literal containing one of them is not supported — use the Julia functions for that. GROUP BY / aggregates in a SELECT string are not supported (use copytable(dst, groupby(…))).\n\n\n\n\n\n","title":"MeasurementSets.taql"},{"category":"method","location":"api/#Base.join-Tuple{AbstractTable, AbstractTable}","page":"API reference","text":"join(left, right; on, rightcols, leftcols=nothing, where=nothing,\n     unmatched=:error, multi=false, oncols=nothing, orderby=nothing) -> GroupedTable\n\nJoin left and right (extends Base.join). Either side is any AbstractTable — a Table, a RefTable, or another groupby / join / query result, so the verbs chain. The default (multi = false) is an N:1 lookup join — each left row matches at most one right row, right columns pulled in per left row (TaQL's JOIN … ON semantics). multi = true is a general M:N equi-join.\n\non is either\n\na column name (String / Symbol) — that left column holds a 0-based row index into right (the MS subtable convention: ANTENNA1 → the ANTENNA subtable row); N:1 only;\na Pair \"LKEY\" => \"RKEY\" — equi-join, matching left.LKEY against right.RKEY (must be unique when multi = false);\na vector of pairs — a composite key (all must match);\na 2-arg predicate (lrow, rrow) -> Bool — a general non-equi join (a MeasurementSets extension; TaQL's own JOIN is == / IN only). lrow / rrow support row.COLNAME. Evaluated by a nested loop over every (left, right) pair — O(nrow(left) × nrow(right)); query / select each side down first for a large table. oncols = (leftnames, rightnames) restricts which columns are loaded onto the predicate rows. unmatched sets the join type as for multi = true; multi itself is ignored;\na string condition — a TaQL-lite expression with L. / R. table-qualified column references (\"L.TIME BETWEEN R.T0 AND R.T1\"). The declarative form of the predicate above — same nested loop, same cost, same unmatched semantics. Must reference at least one L.<col> and one R.<col>; a bare column name is still the index-lookup join.\n\nrightcols lists the right columns to attach — \"NAME\" or \"NAME\" => \"ANT_NAME\" to rename. leftcols (default: every left column) likewise selects/renames left columns. Output names must be unique across both. where filters the assembled result (a string over the output column names, or a row -> Bool closure).\n\nunmatched — for multi = false: :error (default — throw on a dangling key), :drop (exclude that left row), :missing (keep it, right columns missing). For multi = true it sets the join type: :drop = inner, :missing / :left = left outer, :right = right outer, :full = full outer, :error = every left row must match ≥ 1. Unmatched rows on either side get missing in the other table's columns.\n\norderby sorts the result by output column name (\"N\" / \"N\" => :desc). Returns a GroupedTable whose columns are lazy views except where missing-fill forces materialisation.\n\n\n\n\n\n","title":"Base.join"},{"category":"method","location":"api/#Base.delete!-Tuple{Union{AbstractString, AbstractTable}}","page":"API reference","text":"delete!(target; where=nothing) -> Int\n\nRemove rows from the CTDS table at target (a path or an open Table). where is a TaQL-lite WHERE string, a row -> Bool closure, or nothing (every row — leaves a 0-row table). Returns the number of rows removed. Extends Base.delete!.\n\n\n\n\n\n","title":"Base.delete!"},{"category":"method","location":"api/#Base.insert!-Tuple{Union{AbstractString, AbstractTable}}","page":"API reference","text":"insert!(target; values, limit = nothing) -> Int\n\nAppend rows to the CTDS table at target (a path or an open Table). values is one row ([\"A\" => 1, \"B\" => 2.5] or (; A = 1, B = 2.5)), a vector of those, or any Tables.jl source (another table, a query result, a Vector{NamedTuple}). Columns of target not supplied get their default (0 / \"\" / a same-shape zero array). Scalar values are coerced to the target column's element type. Returns the number of rows inserted. Extends Base.insert!.\n\nlimit controls how many rows are appended (TaQL's INSERT … LIMIT): nothing or 0 (default) inserts one row per values row; a positive limit inserts exactly that many, cycling through the values rows; a negative limit inserts nrow(target) + limit rows (also cycling), clamped at zero.\n\n\n\n\n\n","title":"Base.insert!"},{"category":"section","location":"concepts/#Concepts","page":"Concepts","text":"","title":"Concepts"},{"category":"section","location":"concepts/#Tables","page":"Concepts","text":"readtable(path) returns one of three AbstractTable kinds. They all answer the same verbs — column, nrow, columnnames, columndesc, keywords, subtables — and are all Tables.jl sources.\n\nkind what it is\nTable a plain on-disk table (the MS MAIN table and every standard subtable)\nRefTable a persistent row-number reference into a parent table — what a TaQL SELECT … GIVING '<path>' writes; reads delegate to the parent\nConcatTable a virtual row-wise concatenation of same-schema tables — a MultiMS (MMS) MAIN table\n\nThe query verbs add a fourth, GroupedTable — an in-memory columnar result (from query on a result, groupby, or join). It is also an AbstractTable, so a pipeline like query(groupby(join(...))) type-checks and each stage feeds the next.\n\nAn MS directory is opened as a MeasurementSet, which wraps the MAIN Table and lazily opens subtables on demand:\n\nms = MeasurementSet(\"/path/to/my.ms\")\nms.ANTENNA              # subtable(ms, \"ANTENNA\")\nms[:DATA]               # a lazy column of MAIN\nsubtablenames(ms)","title":"Tables"},{"category":"section","location":"concepts/#Storage-managers","page":"Concepts","text":"A storage manager is how casacore lays one column's data out on disk. MeasurementSets reads and writes all of the ones a real MS uses, and picks a sensible one for you when you create a table:\n\nmanager used for\nStandardStMan scalar columns, fixed-shape arrays, strings\nIncrementalStMan slowly-varying scalar metadata (TIME, FIELD_ID, …) — \"store on change\"\nTiledShapeStMan / TiledColumnStMan / TiledCellStMan visibility cubes (DATA, FLAG, WEIGHT_SPECTRUM, UVW)\nDyscoStMan lossy-compressed DATA / WEIGHT_SPECTRUM (the aroffringa/dysco format)\nvirtual engines (ScaledArrayEngine, CompressComplex, …) a column mapped onto a hidden column of scaled integers\nBitFlagsEngine an Array{Bool} column (e.g. FLAG) mapped onto a stored integer, one bit per flag category\nForwardColumnEngine a column that forwards every read to a same-named column in another table (see reference_copy)\nVirtualTaQLColumn a column whose per-row value is a stored TaQL-lite CALC expression over the table's own columns (write_table(...; virtualtaql=Dict(\"CV\" => \"TIME - 4.6e9\")))\n\nColumn data is read lazily — column returns a Column (an AbstractVector); col[i] fetches one cell, col[:] takes the manager's whole-column fast path. You only ever name a manager explicitly to choose a layout when creating a table (write_table(...; tsm=[...], ism=[...], engines=..., dysco=..., storage=...)).\n\nTables can also be packed into a single MultiFile (table.mf) or MultiHDF5 (table.mfh5) container file; readtable detects and resolves through these transparently. MultiHDF5 needs HDF5.jl — do import HDF5 first (it is an optional weak dependency).","title":"Storage managers"},{"category":"section","location":"concepts/#The-query-engine","page":"Concepts","text":"query / groupby / join / update! / taql implement a deliberate subset of TaQL — casacore's Table Query Language. It is enough for real MS filtering, aggregation and joins, and almost every operator / function / clause it accepts is a genuine subset of TaQL's own (checked against TaQL's grammar, and against a live cross-check that runs the same string through both engines). A few conveniences go beyond TaQL — notably join's non-equi forms (on = (lrow, rrow) -> Bool, or a string \"L.T BETWEEN R.T0 AND R.T1\"), which TaQL has no equivalent for.\n\nIt is not a full TaQL implementation. The Changelog (phases 22–31) spells out what each area does and does not support — briefly: 1-based array element/slice indexing (DATA[1,1], V[1:4,1], UVW[-1], V[end-2:end,1]), BETWEEN / NOT BETWEEN, bitwise & | ^ ~ (^ = xor), ~= / !~= approximate equality, and UPDATE … SET col[i,j] = … array-slice / boolean-mask (SET col[maskexpr] = …) / (col, maskcol) paired assignment, and computed query select columns (\"amp\" => \"sqrt(abs(V))\"), and masked arrays (V[boolexpr], marray / arraydata / arraymask, SELECT expr AS (val, mask), masked g* / gs* aggregates; reductions skip masked elements) are supported. So are array literals ([a, b, c]), scientific-notation numbers (1.4e9), quantity literals (1.4GHz, 10arcsec — compared against a column that carries a QuantumUnits keyword; needs the Unitful extension), sexagesimal literals (10h30m, 45d51m16s, 12h, 45d → radians; h = hour angle ×15, d = degrees), and date/time + angle functions (datetime, mjd, mjdtodate, date, time, year/month/day/weekday, cdate/ctime/…, hms/dms, angle (sexagesimal '10h30m' → radians), normangle, angdist/angdistx — dates are an MJD Float64). With import SOFA, mscal.* derived-MS functions — mscal.ha1() / mscal.hadec1() / mscal.azel1() / mscal.az1() / mscal.el1() / mscal.pa1() (parallactic angle) / mscal.last1() (local sidereal time) / mscal.itrf() / mscal.uvw_j2000() / mscal.delay() — computed per MAIN row from TIME + the ANTENNA / FIELD subtables (query(main, \"mscal.el1() > 0.3\"), groupby(main, \"FIELD_ID\"; select = [\"az\" => \"gmean(mscal.az1())\"])). The direction functions take an optional direction argument — a body name (mscal.el1('SUN')), a FIELD direction column (mscal.az1('DELAY_DIR')), a [ra, dec] J2000 pair (radians), or a sexagesimal 'RA, DEC' string (mscal.el1('10h42m31, 45d51m16')). mscal.stokes(col [, 'types'] [, rescale]) converts a DATA / FLAG / WEIGHT array cell between correlation bases ('IQUV' / 'CIRC' / 'LIN' / a comma-list), keyed by POLARIZATION.CORR_TYPE. mscal.<sel>('spec') (baseline / field / spw / scan / state / array / obs) is MSSelection-lite row selection — a comma-list of ids / N~M ranges / name globs, ! to subtract, L & R baselines (query(main, \"mscal.baseline('ea01 & *') AND mscal.field('3C*')\")). mscal.time('t0~t1') (ISO / YYYY/MM/DD endpoints) and mscal.uvdist('a~b[m|km|klambda|…]') select TIME / 2-D uv-distance ranges. mscal.spw('0:5~20') takes an optional :chanlist (channel index ranges a~b/a~b^step or CHAN_FREQ ranges f1~f2GHz); mscal.chan('0:5~20') returns the per-row selected-channel BitVector. mscal.corr('RR,LL') (polarization-setup match) and mscal.feed('0 & 1') (the mscal.baseline form on FEED1/FEED2) round out the selection set.","title":"The query engine"},{"category":"section","location":"concepts/#Physical-units","page":"Concepts","text":"import Unitful, UnitfulAngles, UnitfulAstro loads an extension that maps a column's QuantumUnits keyword onto a Unitful unit: columnunit(t, \"CHAN_FREQ\") → u\"Hz\", qcolumn(t, \"UVW\") → the whole column as … m quantities. UnitfulAngles supplies the angle vocabulary (arcsec, mas, °) and UnitfulAstro the astronomy units (Jy, pc, AU); the extension also registers the dimensionless \"pseudo-units\" casacore uses that no Julia package provides (beam, pixel, lambda, …). Note that angles are SI-dimensionless here (dimension(u\"rad\") == NoDims), unlike casacore where rad/sr are base dimensions — angle↔angle and angle↔scalar conversions still work; use DimensionfulAngles.jl for strict casacore-style dimensional angles. MeasurementSets.UNITS_NO_JULIA_COUNTERPART lists every casacore unit without a third-party Julia implementation and how it is handled. TaQL unit literals (1.4GHz) landed in the query engine.\n\nThe write side is symmetric: a write_table column whose Julia element type is a Unitful quantity (or a Measure — see below) is stored as plain numbers with the right QuantumUnits / MEASINFO keyword stamped automatically, so qcolumn / measure read it straight back. write_table(...; units = Dict(\"CHAN_FREQ\" => \"Hz\")) stamps the keyword explicitly (and overrides the auto-detection for that column).","title":"Physical units"},{"category":"section","location":"concepts/#Reference-frames-(measures)","page":"Concepts","text":"A measure-valued column declares its physical quantity and reference frame in a MEASINFO keyword — TIME is an epoch in UTC, UVW a uvw coordinate in ITRF, ANTENNA.POSITION a position in ITRF, FIELD.PHASE_DIR a direction whose frame is a per-row code (VarRefCol), SPECTRAL_WINDOW.CHAN_FREQ a frequency likewise.\n\nmeasinfo(t, col) parses that keyword; measure(t, col[, row]) reads a cell as a typed value — MEpoch (MJD days), MDirection (radians), MPosition (metres), MFrequency (Hz), MuvW / MBaseline (metres) — carrying its frame as a type parameter (MEpoch{UTC}, MDirection{J2000}, MuvW{ITRF}).\n\nWriting is symmetric: a write_table column of Measure values ([MEpoch{UTC}(...) for ...]) is stored as plain numbers in the canonical unit (epoch → seconds, direction → radians, …) with the MEASINFO + QuantumUnits keyword stamped — no measures= kwarg needed. The auto path is fixed-Ref only (from the first row's frame); a per-row VarRefCol still needs an explicit measures= entry.\n\nimport SOFA loads an extension that converts between frames:\n\nimport SOFA, EarthOrientation          # SOFA alone works; EO adds ΔUT1 / polar motion\n\nfr = MeasFrame(epoch    = measure(main, \"TIME\", 1),\n               position = measure(subtable(ms, \"ANTENNA\"), \"POSITION\", 1),\n               direction = measure(subtable(ms, \"FIELD\"), \"PHASE_DIR\", 1))\n\nmeasconvert(measure(main, \"TIME\", 1), TAI)            # UTC → TAI\nmeasconvert(MDirection{J2000}(2.0, 0.5), AZEL; frame = fr)\nmeasconvert(MFrequency{TOPO}(100e9), LSRK; frame = fr)\nmeasconvert(MRadialVelocity{LSRK}(2e4), BARY; frame = fr)\nmeasconvert(measure(main, \"UVW\", 1), J2000; frame = fr)   # uvw needs frame.direction\n\nEpoch: UTC / TAI / TT / TDB / UT1.\nDirection: J2000 / ICRS / B1950 / APP / GALACTIC / ECLIPTIC / AZEL / AZELGEO / HADEC / ITRF.\nFrequency and radial velocity: TOPO / GEO / BARY / LSRK / LSRD / GALACTO / LGROUP (Local Group, ~308 km/s) / CMB (CMB dipole, ~369.5 km/s).\nBaseline (MBaseline) and uvw (MuvW, the UVW column): rotated between any direction frame; a uvw conversion also needs frame.direction (the phase centre) for the pole rotation.\nSolar-system body (SUN / MOON / MERCURY / VENUS / MARS / JUPITER / SATURN / URANUS / NEPTUNE): a FIELD.PHASE_DIR column can name a body as its frame (a moving target). measure() reads it as MDirection{SUN} (the stored (lon, lat) is a placeholder); measconvert(MDirection{SUN}(0,0), AZEL; frame) resolves the body's geocentric apparent place via SOFA.plan94 / moon98 — accuracy ~arcsec (Sun / Moon / Venus / Mercury) to ~arcmin (Jupiter / Saturn). The Moon's topocentric parallax is applied when frame.position is set. Body frames are source-only (you cannot convert a direction to one). PLUTO has no plan94 entry.\nEphemeris (MeasComet) tables: a FIELD row with a non-negative EPHEMERIS_ID points at an EPHEM<id>_*.tab polynomial position table in the FIELD subtable directory. field_ephemeris opens it; ephemeris_direction / _radvel / _distance evaluate it (linear interpolation of the bracketing rows, matching casacore's MeasComet::get); measure(fld, \"PHASE_DIR\", row; epoch) and the mscal.* functions use it automatically for a moving-target field. Sub-arcmin, so it supersedes plan94 when a real ephemeris is present. Polynomial (numpoly > 0) PHASE_DIR and the DiskLong/DiskLat sub-Earth point are out of scope.\n\nBacked by the pure-Julia SOFA.jl (v2, IAU SOFA port). Without EarthOrientation.jl the conversions run at ~1 arcsecond (ΔUT1 = 0, no polar motion) with a one-time warning. J2000 is treated as ICRS (a ~0.02″ frame-bias simplification). MeasComet / ephemeris tables and pulsar-timing-grade precision are out of scope. TaQL date/time / angle functions and the mscal.* derived-MS functions landed in the query engine (see above).\n\nThe write path takes a measures = keyword on write_table — Dict(\"D\" => (; kind = :direction, ref = \"J2000\")) or the per-row (; kind, varrefcol, tabtypes, tabcodes) form — and copyms / copytable round-trip a column's MEASINFO verbatim.\n\nDoppler shifts are the other spectral axis — the convention (RADIO / OPTICAL / RATIO / BETA / GAMMA) rather than a reference frame. MDoppler{RADIO}(0.01); measconvert(d, OPTICAL) changes convention (pure algebra — no SOFA); and, given a rest frequency, doppler(f, ν₀) / frequency(d, ν₀) / restfrequency(f, d) bridge to/from MFrequency, doppler(v) / radialvelocity(d) to/from MRadialVelocity. A Doppler value is frame-agnostic — measconvert the frequency to the frame you want first, then bridge.\n\nThe bridge functions broadcast, so a whole spectral axis is one call — radialvelocity.(measure(spw, \"CHAN_FREQ\", 1), ν₀) is the velocity of each channel of a spectral window relative to a line rest frequency. shiftfreq(d, νs) multiplies a frequency grid by the Doppler factor √((1−β)/(1+β)) (casacore MDoppler::shiftFrequency).","title":"Reference frames (measures)"},{"category":"section","location":"changelog/#Changelog","page":"Changelog","text":"MeasurementSets.jl is pre-1.0 (1.0.0-DEV). It has been built in numbered phases — each a self-contained slice of functionality, verified against Casacore.jl / real TaQL / a real CASA install before merging. These are development milestones, not tagged releases; the public API has been additive throughout. Newest phases are at the bottom.\n\nFor a task-oriented overview of what the package does today, see the Home.\n\n","title":"Changelog"},{"category":"section","location":"changelog/#Phase-1-—-CTDS-metadata-(read)","page":"Changelog","text":"AipsIO primitive decoder; table.dat / table.info parsing (table & column descriptions, keyword sets incl. nested Records / QuantumUnits / MEASINFO, data-manager bindings, row counts, endianness); the MS subtable tree.","title":"Phase 1 — CTDS metadata (read)"},{"category":"section","location":"changelog/#Phase-2-—-StandardStMan-(SSM)-column-data-(read)","page":"Changelog","text":"getcolumn / getcell for SSM-backed columns: scalar numerics, Bool (bit-unpacked), variable-length String (incl. multi-bucket string buckets), and direct fixed-shape numeric/Bool arrays.","title":"Phase 2 — StandardStMan (SSM) column data (read)"},{"category":"section","location":"changelog/#Phase-3-—-TiledStMan-column-data-(read)","page":"Changelog","text":"getcolumn / getcell for TiledShapeStMan and TiledColumnStMan columns — the visibility cubes (DATA, FLAG, WEIGHT, SIGMA, UVW, …).  Header parsing, row → hypercube mapping, and tile de-interleaving from the table.f<n>_TSM<m> files (mmapped).  Detects never-written columns.  (Multi-column hypercubes: Phase 11.)","title":"Phase 3 — TiledStMan column data (read)"},{"category":"section","location":"changelog/#Phase-4-—-IncrementalStMan-column-data-(read)","page":"Changelog","text":"The \"store-on-change\" manager behind most MAIN metadata columns (TIME, INTERVAL, EXPOSURE, FIELD_ID, …).  With this every column of a typical MAIN table is readable except ones that were never written.","title":"Phase 4 — IncrementalStMan column data (read)"},{"category":"section","location":"changelog/#Phase-5-—-high-level-API,-Tables.jl,-standard-schema","page":"Changelog","text":"Lazy Column <: AbstractVector (t[:DATA], col[i], col[1:5], col[:]), Tables.jl column and row access (subtables drop straight into DataFrame, Tables.rowtable, …), and a machine-readable encoding of the MS v2 standard schema (SCHEMAVER2, stdtable, validate).","title":"Phase 5 — high-level API, Tables.jl, standard schema"},{"category":"section","location":"changelog/#Phase-6-—-writers","page":"Changelog","text":"Pure-Julia table.dat + StandardStMan + TiledShapeStMan writers, little- endian storage-manager files, verified against Casacore.jl.\n\nwrite_table(dir, name, cols; nrow, tsm=…, ism=…) — one CTDS table from name => vector pairs or a Tables source; tsm / ism name the columns to bind to TiledShapeStMan / IncrementalStMan (default: StandardStMan).\ncopyms(src, dst; rows=Colon(), subtables=Colon()) — copy an MS, preserving each column's storage-manager kind.\ncreate_ms(dir; nrow, nchan, ncorr, nant) — synthesise a minimal, validate-clean MS.","title":"Phase 6 — writers"},{"category":"section","location":"changelog/#Phase-7-—-StandardStMan-indirect-/-string-arrays-(read-write)","page":"Changelog","text":"The StIndArray / table.f<n>i mechanism and the string-handler array format behind every variable-shape subtable column (CHAN_FREQ, CORR_TYPE, POLARIZATION_TYPE, POL_RESPONSE, PHASE_DIR, LOG, …). copyms now reproduces every column of a standard MS; create_ms writes these as real ragged arrays.","title":"Phase 7 — StandardStMan indirect / string arrays (read + write)"},{"category":"section","location":"changelog/#Phase-8-—-IncrementalStMan-writer-ISM-indirect-arrays","page":"Changelog","text":"A byte-exact writer for the \"store on change\" run-length format (header, multi-bucket data + index parts, ISMIndex), plus ISM indirect-array read + write. copyms keeps a source's ISM columns as ISM; create_ms writes MAIN's per-integration scalars (TIME, FIELD_ID, …) through ISM.","title":"Phase 8 — IncrementalStMan writer + ISM indirect arrays"},{"category":"section","location":"changelog/#Phase-9-—-in-place-edits","page":"Changelog","text":"edit(path) do t … end opens an existing table for update: overwrite cells / whole columns (t[:X][i] = v, t[:X][:] = vals) and append rows (addrows!(t, n)).  Hybrid persist — a touched TiledShapeStMan column is patched in the tile file in place (editing one cell of a multi-GB cube touches a few bytes); a touched StandardStMan / IncrementalStMan file is regenerated from its in-memory column data.","title":"Phase 9 — in-place edits"},{"category":"section","location":"changelog/#Phase-10-—-row-deletion-addColumn-/-removeColumn","page":"Changelog","text":"edit sessions can now change the row set and the schema:\n\nedit(\"/tmp/copy.ms\") do t\n    removerows!(t, [3, 8, 12])            # drop rows\n    addrows!(t, 5)                        # append rows\n    addcolumn!(t, \"WEIGHT_SPECTRUM\")      # from the standard schema\n    addcolumn!(t, \"FOO\", rand(nrow(t)))   # explicit data\n    removecolumn!(t, \"FLAG_CATEGORY\")\nend\n\nremoverows! / addcolumn! / removecolumn! take the regen persist path: every affected storage-manager file is rebuilt from the resolved in-memory column data (a tiled column's tile file included — so, unlike casacore, rows can be deleted even from a table with a TiledStMan column) and table.dat is rewritten in full.  Untouched managers keep their files and header bytes.  Plain cell/column overwrite and pure row appends still take the Phase-9 in-place fast path.","title":"Phase 10 — row deletion + addColumn / removeColumn"},{"category":"section","location":"changelog/#Phase-11-—-multi-column-tiled-storage-managers","page":"Changelog","text":"TiledShapeStMan hypercubes shared by several data columns of one cell shape (the DATA + FLAG + WEIGHT_SPECTRUM layout of a CASA-filled MS): concatenated per-column tile blocks in casacore's size-sorted order, read and write.  New TiledColumnStMan and TiledCellStMan writers. write_table takes tsm / tcm / tcell column-name groups; copyms reproduces a source's hypercube grouping (and keeps a TiledColumnStMan UVW as such instead of moving it to StandardStMan); create_ms shares one cube between DATA, FLAG and WEIGHT_SPECTRUM.","title":"Phase 11 — multi-column tiled storage managers"},{"category":"section","location":"changelog/#Phase-12-—-virtual-column-engines","page":"Changelog","text":"The other kind of casacore data manager: one that stores nothing itself but maps a column the user sees onto a hidden column of scaled integers, virtual = stored·scale + offset.  Read + write for ScaledArrayEngine, ScaledComplexData, CompressFloat, CompressComplex, CompressComplexSD (and MappedArrayEngine), including auto-scale (per-row scale / offset companion columns computed from each row's range).\n\nwrite_table(\"/tmp/t\", \"T\", [\"DATA\" => cubes]; nrow=n,\n            engines = Dict(\"DATA\" => (; kind = :compresscomplex, autoscale = true)))\n\ncopyms re-encodes and keeps an engine column compressed; edit sessions re-encode a touched engine column on flush.  Verified byte-for-byte against casacore's own decoder for the auto-registered engines (Compress*, MappedArrayEngine).","title":"Phase 12 — virtual column engines"},{"category":"section","location":"changelog/#Phase-13-—-concurrent-access-locking","page":"Changelog","text":"Cooperative fcntl locking + row-count synchronisation via table.lock, so a table is safe to share with another Julia session or a real casa / python-casacore process.  readtable holds a shared lock while it slurps table.dat and trusts the table.lock TableSyncData sync blob's row count over table.dat's (as casacore does); every writer runs under an exclusive lock, writes storage-manager files through an atomic rename, and updates the sync blob (new row count, bumped modify counter) on the way out.\n\nt = readtable(\"/data/my.ms\")\n# ... another process appends rows ...\nis_stale(t)          # true\nt = resync(t)        # re-opens; is_stale(t) now false\nis_multiused(\"/data/my.ms\")   # is anyone else holding it open?\n\nLocking is automatic and degrades to a silent no-op wherever it is unavailable (NFS without a lock daemon, a read-only directory, unsupported OS).  macOS + Linux.\n\nA contended acquire also announces itself in table.lock's cooperative request-id list (and removes itself again once done), the same way a real casacore process would — so a real casa / python-casacore peer holding the table in its default AutoLocking mode can see us waiting and voluntarily release early, rather than us sitting out the full poll timeout (Phase 17).","title":"Phase 13 — concurrent access + locking"},{"category":"section","location":"changelog/#Phase-14-—-reference-and-concatenation-tables-(read)","page":"Changelog","text":"readtable now recognises the other two first-class casacore table kinds and returns a matching view:\n\nRefTable — a persistent row-number reference into a parent table, what a TaQL SELECT ... GIVING '<path>' row selection or table.query writes.  Column reads delegate to the parent through the selected row list; a SELECT a, b AS c rename is honoured.\nConcatTable — a virtual row-wise concatenation of same-schema tables (the MAIN table of a MultiMS).  Row offsets are recomputed from each part's row count on open.\n\nBoth present the same column / nrow / columndesc / keywords / subtables / Tables.jl surface as a plain Table and share the new AbstractTable supertype; is_stale / resync follow through to the parent(s).\n\nrt = readtable(\"/tmp/selection.tab\")     # a TaQL RefTable\nnrow(rt); rt[:DATA][1:10]                # reads the parent's rows","title":"Phase 14 — reference & concatenation tables (read)"},{"category":"section","location":"changelog/#Phase-15-—-persist-a-selection:-write-RefTable-/-ConcatTable,-and-a-data-manager-preserving-materialise","page":"Changelog","text":"# a lightweight reference -- no data copied, openable by casa/python-casacore\nwrite_reftable(\"/tmp/ref.tab\", readtable(\"/tmp/t.tab\"), [5, 1, 3];\n               select = [\"A\" => \"A\", \"BR\" => \"B\"])   # optional rename/projection\nwrite_concattable(\"/tmp/cc.tab\", [readtable(\"/tmp/p0\"), readtable(\"/tmp/p1\")])\n\n# a real independent table, storage-manager / engine layout kept\ncopytable(\"/tmp/plain.tab\", rt)          # rt :: RefTable or ConcatTable\n\ncopytable (and, through it, copyms/write_ms when MAIN or a subtable turns out to be a RefTable/ConcatTable) derives each output column's storage-manager or virtual-engine kind from the source exactly as copyms already did for a plain table — from the parent for a RefTable, from the first part for a ConcatTable — mirroring casacore's own GIVING ... AS PLAIN (dataManagerInfo()).\n\nNot yet implemented: editing a persisted RefTable/ConcatTable in place; concatenating keyword subtables on write; hypercube coordinate / id columns; TiledDataStMan (ForwardColumnEngine and BitFlagsEngine landed in Phase 40, VirtualTaQLColumn in Phase 41); adding a column (or engine) to an existing table in an edit session; in-place per-data-manager resync. (Free-list bucket reuse doesn't apply here — every edit fully regenerates any touched storage-manager file from resolved data, so nothing accumulates across edits the way casacore's own in-place bucket model can.)","title":"Phase 15 — persist a selection: write RefTable / ConcatTable, and a data-manager-preserving materialise"},{"category":"section","location":"changelog/#Phase-16-—-copyms-performance","page":"Changelog","text":"A full, in-order table/subtable copy (every subtable copy, and the default copyms/copytable with no rows= override) now reads each source column via its own whole-column fast path instead of cell by cell — roughly halves the time on a large, mostly-indirect-array subtable (925,645-row POINTING: 68 s → 35 s on the reference MS) and more on a scalar/fixed-shape/tiled-heavy one. A RefTable selection or an explicit partial row range is unaffected.","title":"Phase 16 — copyms performance"},{"category":"section","location":"changelog/#Phase-17-—-cooperative-lock-hand-off","page":"Changelog","text":"Closes the one documented gap in Phase 13's locking: a contended acquire announces this process in table.lock's request-id list and removes itself again once done (casacore LockFile::addReqId/removeReqId), so a real casacore peer holding the table in its default AutoLocking mode can see us waiting and release early. See the Phase 13 section above for the usage example — this is automatic, no API change.","title":"Phase 17 — cooperative lock hand-off"},{"category":"section","location":"changelog/#Phase-18-—-DyscoStMan-column-data-(read)","page":"Changelog","text":"DyscoStMan (the third-party aroffringa/dysco lossy-compression storage manager many large modern MSes use for DATA/WEIGHT_SPECTRUM) is no longer in the \"data manager not yet supported\" set — getcolumn/getcell/column transparently decode it. Scope: AF normalization + TruncatedGaussian quantization only (the real-world default combo), read-only. Verified against a genuine DyscoStMan-compressed table written by a real CASA install's casatools (table.create(...; dminfo=...)) — decoded DATA matches CASA's own getcol() to float32 rounding precision, WEIGHT_SPECTRUM (a plain linear quantizer, no dictionary) matches exactly. copyms/write_ms/copytable of a Dysco-bound column falls back to a plain StandardStMan copy (decode-then-re-encode uncompressed) — writing Dysco is out of scope. Not yet implemented: RF/ Row normalization; Gaussian/Uniform/StudentsT distributions (a clear error, not a silent misdecode); writing/compressing.","title":"Phase 18 — DyscoStMan column data (read)"},{"category":"section","location":"changelog/#Phase-19-—-DyscoStMan-full-read-write","page":"Changelog","text":"Completes Phase 18: all three normalizations (AF/RF/Row) and all four quantization distributions (Gaussian/Uniform/StudentsT/TruncatedGaussian), read and write. write_table/create_ms take dysco=/dysco_spec= kwargs to compress one or more columns (a Dysco group works like a tsm group — one or more column names sharing one instance); copyms/write_ms/ copytable now preserve a source's Dysco compression (reading the live instance's own parameters) instead of downgrading to plain StandardStMan; edit() supports setcell!/addrows!/removerows! on a Dysco-bound column (always via the regen path — a touched cell needs its whole block re-decoded and re-encoded, like a virtual engine, not like a tiled cube's byte-addressable in-place patch). Verified against the real CASA install used in Phase 18: all 24 (normalization × distribution × dither) write combinations produce files real CASA opens and decodes to float32 rounding precision of our own decoder — genuine write-direction interop. dither=true (the default, matching casacore's own always-dither write path) uses Julia's own Random, not casacore's std::mt19937 bit-for-bit — a deliberate departure with no effect on decodability (dithering only affects which of two adjacent quantization symbols an in-between value rounds to). StudentsT's CDF comes from Distributions.jl's TDist (unlike the other three distributions, which use SpecialFunctions.jl's erf/erfinv matching casacore's own math); its inverse CDF is still our own bisection over that CDF (no direct Distributions.jl quantile call), self-consistent rather than GSL-bit-exact.","title":"Phase 19 — DyscoStMan full read + write"},{"category":"section","location":"changelog/#Phase-20-—-MultiFile/MultiHDF5-container-read-support","page":"Changelog","text":"casacore can pack every small per-storage-manager private file of a table (table.f<seq>, table.f<seq>i, table.f<seq>_TSM<k>) into one real file on disk (table.mf or table.mfh5), to cut open-file-descriptor counts and help filesystems like Lustre. Both formats are now transparently readable — readtable detects table.mf/table.mfh5 alongside table.dat and every StandardStMan/IncrementalStMan/ TiledStMan opener resolves its private file through the container instead of a real path (Dysco and the virtual engines are never container-packed, matching real casacore). Read-only; edit() refuses a container-backed table, and copytable/copyms transparently un-pack one into an ordinary plain table. MultiFile is a from-scratch byte-exact port (header, packed/run-length-compressed block index, casacore's own nonstandard CRC32) verified against a real casacore-authored table.mf (TaQL storage=\"multifile\") — genuine interop, cross-checked with Casacore.jl. MultiHDF5 needed adding HDF5.jl as a dependency — the project's first non-pure-Julia dependency, a deliberate, explicit user decision — but has no real casacore oracle on this machine: neither Casacore.jl's bundled casacorecxx_jll nor the CASA.app install used for the Dysco oracle has HDF5 support compiled in, so it is verified only against a self-authored fixture built directly with HDF5.jl following the documented format — a known, standing verification gap. A container-backed virtual file's bytes are mmap'd zero-copy when its blocks are physically contiguous (the common case for a freshly-written, never-edited container; matches this package's existing non-container mmap performance exactly) and materialized otherwise; MultiHDF5 always materializes (no mmap equivalent for HDF5).","title":"Phase 20 — MultiFile/MultiHDF5 container read support"},{"category":"section","location":"changelog/#Phase-21-—-MultiFile/MultiHDF5-container-write-support","page":"Changelog","text":"Completes Phase 20: write_table/create_ms/copytable/copyms/write_ms all take storage=:sepfile|:multifile|:multihdf5 and blocksize= (default 4 MiB, matching casacore's own default) to pack every StandardStMan/ IncrementalStMan/TiledStMan private file into one table.mf/ table.mfh5 — Dysco and virtual-engine files stay separate, matching real casacore. write_ms/copyms's storage= packs every table (MAIN and each subtable), each into its own container, mirroring what a real casacore MS created under a global StorageOption looks like. The writer only ever creates a fresh container in one shot — edit() still refuses a container-backed table (Phase 20's guard). A real find along the way: a container file on disk isn't sufficient by itself for a genuine casacore reopen to use it — casacore's ColumnSet::getFile reads the storage option back out of table.dat's own ColumnSet block (a version-gated field our writer wasn't emitting), not from probing table.mf's presence, so table.dat's ColumnSet block now carries that field too when storage != :sepfile. Verified with genuine write-direction interop for MultiFile (our own writer's output opened and read correctly by real Casacore.jl, mirroring the Dysco Phase 19 write-interop pattern); MultiHDF5 write output has no real-casacore oracle available on this machine (same gap as Phase 20's read side).","title":"Phase 21 — MultiFile/MultiHDF5 container write support"},{"category":"section","location":"changelog/#Phase-22-—-TaQL-lite-query-engine","page":"Changelog","text":"query(t, wherestr) row-filters a Table/RefTable/ConcatTable with a small TaQL-like WHERE string (comparisons, AND/&&, OR/||, NOT/!, parentheses, col IN [v1,v2,...]), reading only the columns the expression references, and returns a RefTable — the same lazy, no-copy view real TaQL's own SELECT ... GIVING produces, persistable unchanged via the existing write_reftable. query(f, t) is the Julia-native counterpart: a predicate closure over a Tables.AbstractRow (query(t; cols=[...]) do row; row.ANTENNA1 > 0; end). Both take a select= for column projection/rename, matching write_reftable's own convention exactly. Every operator/keyword spelling accepted is a genuine subset of real TaQL's own (verified against its lexer, and against a live TaQL cross-check test that runs the same WHERE string through both engines and compares the row selections).","title":"Phase 22 — TaQL-lite query engine"},{"category":"section","location":"changelog/#Phase-23-—-TaQL-lite-ORDER-BY","page":"Changelog","text":"Both query entry points now sort their matched rows before building the result RefTable. In the string form, an optional trailing ORDER BY col [ASC|DESC], ... (bare column references only, verified against TaQL's own sortlist/ sortexpr grammar) — query(t, \"A > 5 ORDER BY B DESC\"), or a bare query(t, \"ORDER BY B\") to sort every row with no filter. In the closure form, a new orderby= keyword takes bare names (ascending) or name => :asc/name => :desc pairs. The sort is a stable multi-key sort (Base.Sort.MergeSort, explicit — ties keep original row order), so a sort key list with several columns tie-breaks left to right. Sort keys are read (and de-duplicated against WHERE-referenced columns) through the same only-what's-needed column resolution Phase 22 established. Cross-checked against real TaQL for row order, not just row-set membership. No arithmetic sort keys, no leading global default-direction shortcut, no NODUPL/DISTINCT.","title":"Phase 23 — TaQL-lite ORDER BY"},{"category":"section","location":"changelog/#Phase-24-—-TaQL-lite-arithmetic-pattern-matching","page":"Changelog","text":"The string parser gains an arithmetic-expression layer between comparison and atom — + - * / % // ** and unary -, at TaQL's own precedence (+ - < * / % // < unary < **, right-assoc) — so a WHERE comparison operand can now be a computed expression: query(t, \"ANTENNA1 % 4 == 0\"), query(t, \"TIME - 4.6e9 > 0 AND (A + B) * 2 <= LIM\"). Arithmetic uses Julia numeric semantics (/ yields a float, // truncates, % is rem). And pattern matching: col LIKE 'pat' / ILIKE / NOT LIKE (SQL glob — % any run, _ one char) and TaQL's col ~ p/glob/, ~ m/regex/, ~ f/regex/ operator (and !~), with / % @ delimiters and an optional trailing i for case-insensitive — each compiled to a Julia Regex mirroring casacore's Regex::fromSQLPattern / fromPattern. The closure form (query(f, t)) is unchanged — arithmetic and matching there are already plain Julia. Cross-checked against real TaQL for a spread of arithmetic and pattern strings.","title":"Phase 24 — TaQL-lite arithmetic + pattern matching"},{"category":"section","location":"changelog/#Phase-25-—-TaQL-lite-functions","page":"Changelog","text":"The string parser gains a curated subset of TaQL's function library — NAME(args...), case-insensitive, with TaQL's own aliases: scalar math (abs, sqrt, exp, log/ln, log10, trig, floor/ceil/round, sign, int, pow, fmod), complex parts (real, imag, arg/phase, conj, norm), array-cell reductions (mean/avg, sum, product, median, variance, stddev, rms, min/max, any, all, ntrue/ nfalse, nelements/count, ndim), string ops (strlength/len, upper/lower, trim/ltrim/rtrim), isnan/isinf/isfinite, iif(cond, a, b), rownumber() (1-based, matching TaQL's default style), pi(), e(). Arithmetic and comparison now also broadcast over an array-cell operand (mean(abs(DATA - MODEL_DATA)) > 3, ntrue(FLAG == True)) — a top-level WHERE that yields an array still errors, exactly as TaQL requires any(...)/all(...) there. New stdlib dependency Statistics. The closure form is unchanged. Cross-checked against real TaQL.","title":"Phase 25 — TaQL-lite functions"},{"category":"section","location":"changelog/#Phase-26-—-GROUP-BY-aggregation","page":"Changelog","text":"A new groupby(t, groupcols; select, where, having, orderby) function — distinct from query because the result shape (one row per group, computed aggregate columns) is not a RefTable. select takes outname => expr_string pairs where each expression may use g-prefixed aggregate functions over the group — gcount(), gsum(x), gmean(x)/gavg(x), gmedian(x), gmin/gmax, gvariance/gsamplevariance, gstddev/gsamplestddev, grms, gany/gall, gntrue/gnfalse, gfirst/glast — plus the group-key columns and any scalar expression of them (gsum(X) / gcount()). An aggregate's argument must reduce to a scalar per row, so array cells compose through the Phase-25 reductions: \"AMP\" => \"gmean(mean(abs(DATA)))\". where pre-filters rows; having filters groups; orderby sorts the result. An empty groupcols gives one whole-table row.\n\nThe result is a GroupedTable — an in-memory Tables.jl source: result.OUTNAME column access, DataFrame(result), and persistence via the existing write_table(dst, \"T\", result; nrow=…) with no new machinery. Cross-checked against real TaQL's own SELECT … GROUP BY ….","title":"Phase 26 — GROUP BY + aggregation"},{"category":"section","location":"changelog/#Phase-27-—-closure-form-groupby","page":"Changelog","text":"For aggregates the g* set can't express, groupby also takes Julia closures — the analog of query(f, t) do row … end. A whole-group do-block returning a NamedTuple (the output row):\n\ngroupby(ms.MAIN, [:ANTENNA1]; cols=[\"ANTENNA1\", \"DATA\", \"WEIGHT\"]) do g\n    (; ANT = first(g.ANTENNA1), N = length(g),\n       WAMP = sum(mean.(abs, g.DATA) .* g.WEIGHT) / sum(g.WEIGHT))\nend\n\nf(g) receives a GroupSlice — g.COLNAME is a materialised vector of that column's values for the group, length(g) the group size. A closure can also appear as a select= RHS alongside strings/symbols (select = [:K => :K, \"N\" => \"gcount()\", \"W\" => g -> sum(g.X.^2)]), and where / having each accept a string or a predicate closure (row -> Bool / g -> Bool). cols= restricts which columns are loaded onto g (default: all of t). Same GroupedTable output.","title":"Phase 27 — closure-form groupby"},{"category":"section","location":"changelog/#Phase-28-—-joins","page":"Changelog","text":"join(left, right; on, rightcols, …) (a method added to Base.join) does an N:1 lookup join — each left row is matched to at most one right row and the chosen right columns are pulled in per left row. This is TaQL's own JOIN … ON semantics, not a general cross product, and it's exactly what an MS needs: ANTENNA1 / FIELD_ID / DATA_DESC_ID in MAIN are keys into the subtables.\n\njoin(ms.MAIN, subtable(ms, \"ANTENNA\");\n     on = \"ANTENNA1\",                       # 0-based row index into ANTENNA\n     rightcols = [\"NAME\" => \"ANT_NAME\", \"POSITION\" => \"ANT_POS\"],\n     where = \"ANT_NAME ~ p/DA*/\")\n\non is a column name (that left column is a 0-based row index into right — the MS convention), a \"LKEY\" => \"RKEY\" pair (equi-join on a unique right key), or a vector of pairs (composite key). rightcols / leftcols pick and rename columns (\"src\" => \"out\", DataFrames-style; leftcols defaults to every left column). where filters the assembled result (a string over the output names, or a row -> Bool closure); orderby sorts it; unmatched is :error (default — throw on a dangling key), :drop, or :missing. The result is a GroupedTable whose columns are lazy MappedColumn views (zero-copy even joining onto MAIN), cross-checked against real TaQL's JOIN.","title":"Phase 28 — joins"},{"category":"section","location":"changelog/#Phase-29-—-chainable-results","page":"Changelog","text":"GroupedTable (the result of groupby, join, or query on one of those) is itself an AbstractTable, so every query verb feeds the next — a full pipeline:\n\njoin(ms.MAIN, subtable(ms, \"ANTENNA\"); on=\"ANTENNA1\",\n     rightcols=[\"NAME\" => \"ANT\"]) |>\n  r -> groupby(r, \"ANT\"; select=[\"ANT\" => :ANT, \"N\" => \"gcount()\",\n                                 \"AMP\" => \"gmean(mean(abs(DATA)))\"]) |>\n  r -> query(r, \"N > 100 ORDER BY AMP DESC\")\n\nquery on a GroupedTable returns a materialised GroupedTable (\"in-memory in, in-memory out\"); groupby / join on one behave exactly as on a disk table. Persist any result with write_table(dst, \"T\", r; nrow=nrow(r)).","title":"Phase 29 — chainable results"},{"category":"section","location":"changelog/#Phase-30-—-the-write-commands","page":"Changelog","text":"UPDATE / DELETE / SELECT INTO, over the Phase 9-11 edit primitives:\n\nupdate!(\"/path/to.ms/ANTENNA\"; set=[\"MOUNT\" => \"'ALT-AZ'\"], where=\"STATION ~ p/PM*/\")\ndelete!(subtable(ms, \"FLAG_CMD\"); where=\"APPLIED\")\ncopytable(\"/tmp/cal.tab\", query(ms.MAIN, \"FIELD_ID == 3\"))          # SELECT ... INTO\n\nupdate!(target; set, where) — target is a path or an open Table / subtable(…); set is \"COL\" => \"expr\" pairs (TaQL-lite expressions over the row's columns, evaluated against the pre-update values, so [\"A\" => \"B\", \"B\" => \"A\"] swaps); where is a WHERE string, a row -> Bool closure, or nothing (every row). Returns the row count changed. delete!(target; where) extends Base.delete!; where=nothing empties the table. copytable(dst, result) persists any query / groupby / join result (DM-preserving for a RefTable, materialised for a GroupedTable) — that is SELECT … INTO.","title":"Phase 30 — the write commands"},{"category":"section","location":"changelog/#Phase-31-—-INSERT","page":"Changelog","text":"insert!(target; values) appends rows, over the same edit / addrows! primitives:\n\ninsert!(\"/path/to.ms/ANTENNA\"; values=(; NAME=\"DA99\", DISH_DIAMETER=12.0))\ninsert!(ms.MAIN, query(other.MAIN, \"FIELD_ID == 3\"))               # INSERT ... SELECT\n\nvalues is one row ([\"A\" => 1, \"B\" => 2.5] or (; A=1, B=2.5)), a vector of those, or any Tables.jl source (another table, a query / groupby / join result, a Vector{NamedTuple}). Unsupplied columns take their default (0 / \"\" / a same-shape zero array); scalars are coerced to the target column's type. Extends Base.insert!. Returns the row count inserted.\n\nA taql(target, \"…\") string-command dispatcher wraps all four: taql(t, \"UPDATE t SET UVW = UVW * 2 WHERE ANTENNA1 == 0\"), taql(t, \"DELETE FROM t WHERE FLAG_ROW\"), taql(t, \"SELECT A, B AS BB WHERE A > 5 INTO '/tmp/out'\"), taql(t, \"INSERT INTO t (A, B) VALUES (1, 2.5), (3, 4.5)\") (VALUES must be constant expressions).\n\nStill, across Phases 22–31 (array indexing landed in Phase 42, BETWEEN in Phase 43, bitwise in Phase 46, ~= in Phase 47): no units, date/time or measures functions, the gs* per-element aggregates, INSERT LIMIT, or UPDATE array-slice assignment.","title":"Phase 31 — INSERT"},{"category":"section","location":"changelog/#Phase-32-—-docs-/-consolidation-pass","page":"Changelog","text":"Documentation, no behaviour change: a docstring on every one of the 53 exported bindings (?nrow, ?Table, ?query, …); the README cut from a 31-paragraph phase log to a task-oriented \"At a glance\" + \"Concepts\" + \"Usage\", with the phase-by-phase history moved to this CHANGELOG. Light code consolidation — the two byte-identical _arrayfile! bodies now share one helper; the casacore/TaQL tableCommand cross-check boilerplate (six copies across four test files) collapsed to one _taqlcmd helper.","title":"Phase 32 — docs / consolidation pass"},{"category":"section","location":"changelog/#Phase-33-—-Documenter.jl-site-(local-build)","page":"Changelog","text":"A docs/ tree — make.jl + Project.toml + Home / Concepts / Guide / API reference / Changelog pages — that builds with julia --project=docs docs/make.jl (output in docs/build/, not deployed). The API page is curated @docs blocks covering all 53 exports (checkdocs = :exported). No deploydocs / CI workflow / badge yet (the GitHub repo slug is still a placeholder).","title":"Phase 33 — Documenter.jl site (local build)"},{"category":"section","location":"changelog/#Phase-34-—-half-precision-(ComplexF16)-MAIN-reads","page":"Changelog","text":"MS visibility data is stored on disk as ComplexF32 (a historical choice) but derives from 8-bit-integer samples, so ComplexF16 loses no real information and halves the working-set size. A MAIN table's TpComplex columns (DATA, MODEL_DATA, CORRECTED_DATA, …) now read back as ComplexF16 by default; the on-disk bytes are unchanged.\n\nt = readtable(\"my.ms\")\neltype(column(t, \"DATA\"))                    # Matrix{ComplexF16}\neltype(column(t, \"DATA\"; precision=:full))   # Matrix{ComplexF32}\nreadtable(\"my.ms\"; precision=:full)          # every column wide\n\nTpFloat columns (WEIGHT, SIGMA, WEIGHT_SPECTRUM) stay Float32 — real weights routinely exceed Float16's 65504 range; Float64 (TIME, UVW), ComplexF64 and Bool are never narrowed. Non-MAIN tables default to :full. readtable(...; precision=…), MeasurementSet(...; precision=…) and column(t, name; precision=…) override; an explicit column(...; precision=:half) narrows a TpFloat column too (the caller's risk). copyms / write_ms / copytable and edit always read the source at full precision, so copies stay byte-exact.","title":"Phase 34 — half-precision (ComplexF16) MAIN reads"},{"category":"section","location":"changelog/#Phase-35-—-decode-straight-into-the-narrow-type","page":"Changelog","text":"A follow-up to Phase 34: the whole-column read (column(t, \"DATA\")[:]) now threads the target element type through the storage-manager decode path (a new astype keyword on the getcolumn family — TiledStMan, DyscoStMan, StandardStMan, the virtual engines, IncrementalStMan), so a narrowed column decodes straight into a ComplexF16 buffer instead of building a ComplexF32 one and converting. No API or value change; the :full path (astype === nothing) is byte-identical to before. The result buffer is half-size and there is no wide transient. Per-cell reads (col[i]) keep the cheap post-convert.","title":"Phase 35 — decode straight into the narrow type"},{"category":"section","location":"changelog/#Phase-36-—-BFloat16-support","page":"Changelog","text":"precision now accepts a type: Float16 / BFloat16 / Float32 (with :half / :full the existing default-behaviour aliases). New dependency: BFloat16s.jl.\n\nreadtable(\"my.ms\"; precision=BFloat16)    # DATA -> Complex{BFloat16}, WEIGHT -> BFloat16\n\nBFloat16 has Float32's full exponent range, so it narrows WEIGHT / SIGMA / WEIGHT_SPECTRUM without the overflow that keeps them at Float32 under :half / Float16 — and its 7-bit mantissa matches 8-bit-derived data. readtable(ms; precision=BFloat16) (or MeasurementSet(...; precision=BFloat16), or column(t, name; precision=BFloat16)) narrows every TpFloat→BFloat16 and TpComplex→Complex{BFloat16} MAIN column. The default is unchanged — DATA → ComplexF16, WEIGHT stays Float32. Float64 / ComplexF64 / Bool are never narrowed. Copies and edits still read the source at full precision; a BFloat16 column writes back as TpFloat (upcast to Float32 on disk).","title":"Phase 36 — BFloat16 support"},{"category":"section","location":"changelog/#Phase-37-—-HDF5-is-now-a-weak-dependency","page":"Changelog","text":"HDF5.jl (a wrapper over the C libhdf5) was a hard dependency since Phase 20, only ever used for MultiHDF5 (table.mfh5) container tables. It is now a weak dependency: the MultiHDF5 code lives in ext/HDF5Ext.jl, a package extension that loads only when you import HDF5 yourself. A plain using MeasurementSets pulls in no C libraries. Reading or writing a table.mfh5 without HDF5 loaded raises a clear, actionable error (... runimport HDF5first). MultiFile (table.mf, pure Julia) is unaffected.","title":"Phase 37 — HDF5 is now a weak dependency"},{"category":"section","location":"changelog/#Phase-39-—-renamed-MeasurementSetv2-MeasurementSets","page":"Changelog","text":"The package, module and repo are now MeasurementSets (plural). The \"v2\" was version noise. The plural keeps struct MeasurementSet (the MeasurementSet(path) MS-directory wrapper) from colliding with the module — using MeasurementSets; ms = MeasurementSet(\"/path\") — the idiomatic Julia split (Dates/Date). Pure rename: same UUID, no API or behaviour change, all tests green. The test-only environment variables are now MEASUREMENTSETS_TEST_MS / MEASUREMENTSETS_CASA_PYTHON, and the HDF5 extension is HDF5Ext.","title":"Phase 39 — renamed MeasurementSetv2 → MeasurementSets"},{"category":"section","location":"changelog/#Phase-40-—-BitFlagsEngine-ForwardColumnEngine","page":"Changelog","text":"The two remaining virtual engines Phase 12 left out.\n\nBitFlagsEngine<StoredType> — an Array{Bool} column (e.g. FLAG) mapped onto a stored integer column, one bit per flag category: virtual[i] = (stored[i] & readMask) != 0. Read + write. The mask is either the numeric _BitFlagsEngine_ReadMask keyword or, when ReadMaskKeys names entries in the stored column's FLAGSETS record, the OR of those. Write stores raw 0/1 (matching casacore's actual putArray — the write mask is not applied). Create one with write_table(dir, \"T\", [\"FLAG\" => cubes]; engines = Dict(\"FLAG\" => (; kind = MeasurementSets.BitFlags(), stored_type = MeasurementSets.TpInt))); copyms / copytable preserve it.\n\nForwardColumnEngine — a column that forwards every read to a same-named column in another table (no data of its own). reference_copy(dst, src; writable = [...]) builds a table whose columns are all forwards to src, except those named in writable (real independent copies) — casacore's MSTableImpl::referenceCopy. edit of a forward table is refused (edit the source); copyms / copytable materialise the forwards into a plain independent table.\n\nRetypedArrayEngine and ForwardColumnIndexedRowEngine stay unsupported — a clear error names them; neither occurs in a standard MS.","title":"Phase 40 — BitFlagsEngine + ForwardColumnEngine"},{"category":"section","location":"changelog/#Phase-41-—-VirtualTaQLColumn","page":"Changelog","text":"casacore's \"CALC column\": a column whose per-row value is a stored TaQL expression evaluated against the table's own columns (a constant-valued MS column, or on-the-fly derived data). Read + write.\n\nRead evaluates the stored _VirtualTaQLEngine_CalcExpr keyword with the Phase 22-25 TaQL-lite engine — a constant expression is computed once, a column-referencing one per row. An expression using a TaQL feature TaQL-lite doesn't support (units, date/time or measures functions) raises a clear ArgumentError naming the column and expression when that column is read — the rest of the table opens fine.\n\nWrite: write_table(dir, \"T\", [..., \"CV\" => zeros(n)]; nrow = n, virtualtaql = Dict(\"CV\" => \"TIME - 4.6e9\")) declares the column and stores the expression (the passed values are ignored). copyms / copytable preserve a VirtualTaQLColumn by re-emitting the expression. edit of such a table is refused (edit the source columns). CCT.Table reads a VirtualTaQLColumn our writer produces — genuine round-trip interop, since casacore auto-registers the engine.","title":"Phase 41 — VirtualTaQLColumn"},{"category":"section","location":"changelog/#Phase-42-—-TaQL-lite-array-indexing-slices","page":"Changelog","text":"Array-cell element and slice indexing in every TaQL-lite expression: UVW[3], FLAG[1,1], DATA[1:4,1], V[:,1], V[1:8:2,1], X[1][2] (chained), V[rownumber(),1] (expression subscripts). 1-based — matching casacore's default TaQL style (the tableCommand cross-checks), Julia, and TaQL-lite's 1-based rownumber(). A colon range is casacore's start:end:step (end before step), inclusive both ends; a bare axis / trailing comma / missing trailing axes = whole axis; a scalar subscript drops that dimension.\n\nThis lands in query / groupby / join WHERE and SELECT, the update! SET RHS, and — for free — VirtualTaQLColumn CALC expressions (Phase 41). Verified against real TaQL via tableCommand (1-based, inclusive-range and end-before-step all agree) and, for the engine path, against CCT.Table decoding virtualtaql=Dict(\"W\" => \"UVW[3]\").\n\nNot supported: boolean-mask subscripts (DATA[FLAG]) and assigning into an indexed cell. (Negative / end-relative indices landed in Phase 44.)","title":"Phase 42 — TaQL-lite array indexing + slices"},{"category":"section","location":"changelog/#Phase-43-—-TaQL-lite-BETWEEN","page":"Changelog","text":"x BETWEEN lo AND hi (inclusive both ends, matching casacore's left/right-closed range) and x NOT BETWEEN lo AND hi, in every TaQL-lite expression surface. lo/hi are arithmetic expressions (B BETWEEN A - 1 AND A); x may be an array cell (elementwise). Binds at the comparison level, so x BETWEEN a AND b OR c groups as (x BETWEEN a AND b) OR c. Verified against real TaQL via tableCommand.","title":"Phase 43 — TaQL-lite BETWEEN"},{"category":"section","location":"changelog/#Phase-44-—-TaQL-lite-negative-/-end-relative-array-indices","page":"Changelog","text":"V[-1] / V[-2,1] — a negative subscript counts from the end (-1 == last), matching casacore's Slicer semantics, and sum(V[-2:-1,1]) for the last two along an axis. Plus an end keyword (Julia-idiomatic, no casacore equivalent) usable in any subscript expression: V[end,1], V[end-2:end,1] — end resolves to that axis's length. end outside a subscript raises a clear error. A non-positive slice step is rejected (casacore does too).\n\nNegative indices are cross-checked against real TaQL via tableCommand; end is exercised only against a hand-computed reference (no TaQL equivalent).","title":"Phase 44 — TaQL-lite negative / end-relative array indices"},{"category":"section","location":"changelog/#Phase-45-—-committed-small-MS-test-fixture","page":"Changelog","text":"The data-dependent tests used to run only where a hard-coded 20 GB real ALMA MS lived. They now run everywhere against a small committed fixture — test/data/sample.ms (~4 MB), a 600-row copyms slice of a real MS (POINTING / SYSPOWER capped at 150 rows; every other subtable kept whole; the SM mix and (4,64) cell shape preserved). Regenerate it with test/gen_sample_ms.jl.\n\nwrite_ms / copyms gained a subtable_rows kwarg (subtable name → row range) for the slicing. Bugfix along the way: write_ms / create_ms wrote subtable-keyword paths as ./NAME (a sibling) instead of casacore's ././NAME (inside the table dir) — our own lenient reader coped, but CCT.Table(ms).ANTENNA (real casacore MS subtable access) failed. Now fixed. SAMPLE_MS defaults to the fixture; MEASUREMENTSETS_TEST_MS still points the tests at a full MS when set — the row/dimension assertions were made relative to nrow(t) so both work. (FLAG_CATEGORY / WEIGHT_SPECTRUM are defined-but-never-written in the source and don't survive a copyms; the fixture re-adds the schema-required FLAG_CATEGORY as an empty column.)","title":"Phase 45 — committed small MS test fixture"},{"category":"section","location":"changelog/#Phase-46-—-TaQL-lite-bitwise-operators","page":"Changelog","text":"Binary & | ^ and unary ~, in every TaQL-lite expression surface. Integer bitwise semantics; elementwise over an array cell. Precedence matches casacore: above comparisons, below +/-, with | < ^ < &. ^ is xor (not exponentiation) — ** stays for power; the old \"^ is not supported, use **\" error is gone. A bare ~ before a p/…/ m/…/ f/…/ literal is still the glob/regex match operator (the tokenizer only treats ~ as bitnot when no pattern literal follows). Verified against real TaQL via tableCommand (precedence, ^=xor, ~=bitnot all agree).","title":"Phase 46 — TaQL-lite bitwise operators"},{"category":"section","location":"changelog/#Phase-47-—-TaQL-lite-approximate-equality-(-/-!)","page":"Changelog","text":"x ~= y / x !~= y — casacore's near(x, y, 1e-5) and its negation: a relative tolerance, with the same zero / opposite-sign special cases casacore uses (Math.cc / Complex.cc). Real, complex, and array-cell operands. Verified against real TaQL via tableCommand. One deliberate divergence: casacore's near(Int, Int) compares |a|-|b| rather than |a-b| (making 3 ~= 4 true) — TaQL-lite uses the same relative form for integers as for floats.","title":"Phase 47 — TaQL-lite approximate equality (~= / !~=)"},{"category":"section","location":"changelog/#Phase-48-—-groupby-ROLLUP","page":"Changelog","text":"groupby(t, cols; select, rollup = true) (and the closure form) adds SQL GROUP BY ROLLUP subtotal rows: the detailed groups, then one level per trailing key dropped, ending with the grand total. In a subtotal row the aggregated-away key columns are missing (so those result columns become Union{T, Missing}). A bare key select entry emits missing; a closure builds its key fields from the new g.keys NamedTuple — (; g.keys..., N = length(g)) — which already carries the missings. GroupSlice also gained g.level (active-key count).\n\ncasacore parses GROUP BY ROLLUP but throws \"not supported yet\", so this is plain SQL semantics with no real-TaQL cross-check.","title":"Phase 48 — groupby ROLLUP"},{"category":"section","location":"changelog/#Phase-50-—-groupby-CUBE-/-GROUPING-SETS","page":"Changelog","text":"Generalises Phase 48's rollup: groupby(t, cols; select, cube = true) computes every key subset (not just prefixes); grouping_sets = [(\"K1\",\"K2\"), (\"K1\",), ()] computes exactly the sets you name (() = grand total). At most one of rollup / cube / grouping_sets. Aggregated-away keys are missing as before. GroupSlice now carries the active-key set (not a prefix count): g.keys / g.level still work, and a new g.grouping NamedTuple gives SQL GROUPING() — true for a key rolled up in that row (also a string-grammar function since Phase 51). casacore parses these but doesn't implement them; plain SQL semantics, no cross-check.","title":"Phase 50 — groupby CUBE / GROUPING SETS"},{"category":"section","location":"changelog/#Phase-51-—-GROUPING()-in-the-string-grammar","page":"Changelog","text":"GROUPING(K) is now a function usable in a groupby select or having string (not just g.grouping.K in a closure) — true when key K is rolled up in that row. The classic uses: select = [\"label\" => \"iif(GROUPING(K2), 'ALL', K2)\", …] and having = \"GROUPING(K1) == 0\". It takes exactly one bare grouping-key column name, is resolved to a constant per grouping set (an AST rewrite, like Phase 44's end), and is rejected outside a group context (in a plain query / a where).","title":"Phase 51 — GROUPING() in the string grammar"},{"category":"section","location":"changelog/#Phase-49-—-M:N-joins","page":"Changelog","text":"join(left, right; on = \"LK\" => \"RK\", ..., multi = true) is a general M:N equi-join — one left row can match many right rows. unmatched sets the join type: :drop = inner, :missing / :left = left outer, :right = right outer, :full = full outer, :error = every left row must match ≥ 1. Unmatched rows on either side get missing in the other table's columns (those output columns materialise; matched columns stay lazy MappedColumn views). Row order is left-row order, then right-match order, then the unmatched right rows.\n\nThe default (multi = false) is unchanged — the Phase-28 N:1 lookup join, still the only form that takes an index-lookup on (a bare column name) or requires a unique right key.","title":"Phase 49 — M:N joins"},{"category":"section","location":"changelog/#Phase-52-—-gs*-per-element-aggregates","page":"Changelog","text":"groupby select strings gained the s-suffixed aggregate variants (gsums, gproducts, gmeans / gavgs, gvariances / gsamplevariances, gstddevs / gsamplestddevs, grmss, gmins, gmaxs, ganys, galls, gntrues, gnfalses). Where gmean(x) reduces one scalar-per-row value over a group, gmeans(x) takes the group's array cells (all the same shape) and reduces them elementwise, giving one array — e.g. \"gmeans(V)\" is the per-cell mean spectrum over a group. Reuses the existing TQLAggr node; no parser or AST change. Cross-checked against real casacore for gsums / gmeans / gmaxs / gstddevs.","title":"Phase 52 — gs* per-element aggregates"},{"category":"section","location":"changelog/#Phase-53-—-INSERT-…-LIMIT","page":"Changelog","text":"insert!(target; values, limit = nothing) gained a limit kwarg (TaQL's INSERT … LIMIT n). nothing / 0 keeps the old behaviour (one row per values row); a positive limit appends exactly that many rows, cycling through the values rows; a negative limit appends nrow(target) + limit rows (also cycling), clamped at zero — matching casacore's own nrow = table.nrow() + limit_p. The taql string form parses both INSERT INTO t … VALUES (…) LIMIT n (trailing) and INSERT LIMIT n INTO t … (prefix); the lite-only INSERT … SET form accepts LIMIT in either position too. Cross-checked against real TaQL for the VALUES forms (positive, cycling, prefix, and negative limits).","title":"Phase 53 — INSERT … LIMIT"},{"category":"section","location":"changelog/#Phase-54-—-UPDATE-array-slice-assignment","page":"Changelog","text":"update!'s set key can now be an array-slice target, \"COL[subscripts]\" => \"expr\" (TaQL's UPDATE … SET NAME[i,j] = …) — 1-based, with end-relative and range subscripts as in query. It reads the target cell at full precision, writes only the addressed sub-region, and leaves the rest of the cell (and unmatched rows) untouched; a scalar RHS fills the slice. Multiple slice assignments to one column in a single update! are applied in order to the same cell (a later one doesn't clobber an earlier one), and a whole-column assignment may precede slice assignments to the same column. taql's UPDATE … SET parses the bracketed LHS. Cross-checked against real TaQL for scalar, range, negative-index, and cross-referencing slice RHS. Boolean-mask subscripts and the (col, maskcol) = … paired form remain non-goals.","title":"Phase 54 — UPDATE array-slice assignment"},{"category":"section","location":"changelog/#Phase-55-—-UPDATE-boolean-mask-assignment","page":"Changelog","text":"update! / taql's UPDATE … SET now also accept a boolean-mask subscript, SET col[maskexpr] = expr — assign only where the mask (a Bool array conforming to the cell, a column or an inline expression like V > 5.0) is true. The slice + mask two-bracket form works in either order: col[slice][mask] (mask conforms to the section) and col[mask][slice] (mask conforms to the cell, then sliced) — matching casacore's maskFirst semantics. Single array subscripts in the query grammar are now full expressions (so V[V > 5] parses), not just arithmetic. The (col, maskcol) = expr paired form raises a clear error — it needs masked-array-producing expressions, which TaQL-lite does not have. Cross-checked against real TaQL for the mask, inline-mask and slice+mask forms.","title":"Phase 55 — UPDATE boolean-mask assignment"},{"category":"section","location":"changelog/#Phase-56-—-general-non-equi-join-(predicate-closure)","page":"Changelog","text":"join's on may now be a 2-arg predicate (lrow, rrow) -> Bool — a range / inequality / tolerance join, evaluated by a nested loop over every (left, right) row pair. lrow / rrow support row.COLNAME. unmatched sets the join type (:drop inner, :missing / :left, :right, :full, :error) exactly as multi = true; oncols = (leftnames, rightnames) restricts which columns are loaded onto the predicate rows. It composes with where / orderby / leftcols / rightcols like the other join forms. This is a MeasurementSets extension — TaQL's own JOIN … ON is == / IN only, so there is no cross-check — and it is O(nrow(left) × nrow(right)): query / select each side down first for a large table.","title":"Phase 56 — general non-equi join (predicate closure)"},{"category":"section","location":"changelog/#Phase-57-—-computed-output-columns-in-query's-select","page":"Changelog","text":"query's select \"out\" => rhs pairs now accept a computed expression as the rhs (\"X * 2\", \"sqrt(abs(V))\", \"iif(K==0, 1, 0)\" — the WHERE grammar, aggregates excepted). When every rhs is a bare column name the result is still a lazy RefTable (unchanged); when any is computed the result is an in-memory GroupedTable with those columns evaluated per matched row. Works for the closure form and on a GroupedTable input too. taql's SELECT parses expr AS alias (a computed column needs the AS). An aggregate in a query select raises a clear error pointing at groupby.","title":"Phase 57 — computed output columns in query's select"},{"category":"section","location":"changelog/#Phase-58-—-GroupedTable-as-a-groupby-/-join-input","page":"Changelog","text":"Since GroupedTable <: AbstractTable (Phase 29) and the query verbs are generic, a groupby / join / query result already feeds straight back into groupby or join — this phase adds a dedicated test set (both sides GroupedTable, index-lookup / equi / multi / predicate on, closure groupby with cols=, a computed-select result as input, 3-verb deep chains, write_table round-trip of the final result) and drops the stale \"use Tables.columntable\" non-goal. No code change. edit / write_ms / copyms-as-MAIN still need a plain on-disk Table.","title":"Phase 58 — GroupedTable as a groupby / join input"},{"category":"section","location":"changelog/#Phase-59-—-UPDATE-SET-(col,-maskcol)-expr","page":"Changelog","text":"update!'s set key can be a (datacol, maskcol) tuple (TaQL's SET (NAME, MASKNAME) = …). (\"D\", \"M\") => \"dexpr\" writes dexpr to D and a non-finite flag (!isfinite, element-wise) of the result to M; (\"D\", \"M\") => (\"dexpr\", \"mexpr\") writes an explicit mask expression to M instead. Either name may be a slice / mask target. taql's UPDATE … SET parses (D, M) = dexpr and (D, M) = (dexpr, mexpr). Since TaQL-lite has no masked-array expressions the data and mask are given explicitly — a documented divergence from casacore, which writes expr's own attached mask, so there is no cross-check. New nonfinite(x) / isnonfinite(x) function (element-wise !isfinite) is available in expressions generally.","title":"Phase 59 — UPDATE SET (col, maskcol) = expr"},{"category":"section","location":"changelog/#Phase-60-—-masked-arrays-in-the-expression-engine","page":"Changelog","text":"A TQLMArray value type (data + a true-means-masked-out Bool mask, casacore's MArray). V[boolexpr] in an expression now yields the whole cell with !boolexpr masked out (not a flat selection); marray(d, m) / arraydata(m) / arraymask(m) build and unpack one. Reductions (sum / mean / min / max / median / variance / stddev / rms / any / all / ntrue / nfalse) skip masked elements, nelements counts the unmasked ones, and arithmetic propagates the mask (union). So mean(V[!FLAG]), gmax(mean(V[FLAG])) (a masked reduction inside a g* aggregate), and SET (D, M) = V[goodcond] (writes V's data and the !goodcond mask to the companion column — the faithful form Phase 59 approximated) all work. A computed select column of masked arrays persists as plain data (arraymask(expr) for a separate mask column).","title":"Phase 60 — masked arrays in the expression engine"},{"category":"section","location":"changelog/#Phase-61-—-SELECT-expr-AS-(val,-mask)","page":"Changelog","text":"query's select (\"valname\", \"maskname\") => \"expr\" pair form (and taql's SELECT expr AS (v, m)) emits two output columns from a masked-array expression — the data and the mask. So query(t; select = [(\"D\", \"F\") => \"marray(DATA, FLAG)\"]) reads a column together with its mask column, and taql(t, \"SELECT V[V > 0] AS (D, M)\") splits a masked selection into data + mask. A non-masked RHS gives a non-finite flag for the mask column (consistent with Phase 59). casacore has no on-disk column↔mask association — the mask is always named, so marray(col, maskcol) is \"read a masked column\".","title":"Phase 61 — SELECT expr AS (val, mask)"},{"category":"section","location":"changelog/#Phase-62-—-masked-g*-aggregates-(first-class)","page":"Changelog","text":"A g* aggregate with a masked-array argument now reduces over the group's unmasked elements: scalar g* (gmean / gsum / gmin / gmax / gmedian / gstddev / grms / gany / gall / gproduct) pools every row's unmasked elements into one flat reduction — gmean(V[!FLAG]) is the mean of every unflagged visibility in the group — and per-element gs* reduces per cell position over the rows where that cell is unmasked (gmeans(V[!FLAG]) = the per-cell mean spectrum, flagged cells ignored; an all-masked cell → NaN/0/false). The 14 Phase-52 gs* closures collapse to one generic _perelem_reduce sharing the non-s scalar reducer (so gproducts is a genuine per-cell product, no matmul). NOT now broadcasts, so V[!FLAG] and NOT FLAG negate an array-cell mask elementwise. casacore's own masked-g* semantics are murky (no reliable cross-check) — the pooled-unmasked interpretation is the clean one.","title":"Phase 62 — masked g* aggregates (first-class)"},{"category":"section","location":"changelog/#Phase-63-—-string-L.-/-R.-qualified-non-equi-join-condition","page":"Changelog","text":"join's on can now be a string — a TaQL-lite expression with L. / R. table-qualified column references, join(l, r; on = \"L.TIME BETWEEN R.T0 AND R.T1\"). It is the declarative form of the Phase-56 predicate closure: same nested loop, same O(N×M) cost, same unmatched join-type semantics. Must reference at least one L.<col> and one R.<col> (aggregates rejected); a bare-identifier on string is still the Phase-28 index-lookup join. The tokenizer now lexes one optional .suffix on an identifier. TaQL has no cartesian non-equi join, so no cross-check.","title":"Phase 63 — string L. / R.-qualified non-equi join condition"},{"category":"section","location":"changelog/#Phase-64-—-src/-restructured-into-subdirectories","page":"Changelog","text":"Pure refactor, no behaviour change. src/ is now organised into io/ (the aips.jl / lock.jl wire-format codecs), tables/ (the CTDS type system + table model + I/O: typeenum.jl, record.jl, table.jl, column.jl, interface.jl, writer.jl, create.jl, edit.jl, resync.jl), datamanagers/ (unchanged), and taql/ (below); only the module file and the two MS-domain files measurementset.jl / schema.jl stay at the top. MeasurementSets.jl's include list keeps its existing order (dependencies force table files to interleave with the datamanagers/ and taql/ includes), just gaining directory prefixes.\n\nThe ~2300-line src/query.jl and src/write_commands.jl become the src/taql/ directory: ast.jl (nodes + visitors + _bcast + masked arrays + indexing), parse.jl (tokenizer + parser + operator tables + pattern→regex), functions.jl (function / aggregate registries + _make_func), query.jl (ORDER BY + select + query(::AbstractTable)), groupby.jl (GroupSlice / GroupedTable / groupby + _geval), join.jl, and commands.jl (update! / delete! / insert! / taql). taql/taql.jl includes the first six; commands.jl stays included last (it builds on edit / create / resync). Test files renamed to taql_query_tests.jl / taql_command_tests.jl. The _select_spec helper also moved from table.jl to taql/query.jl, so the whole query engine — parser, evaluators, verbs, write commands — now lives under src/taql/; create.jl / resync.jl / edit.jl keep only thin GroupedTable / VirtualTaQLColumn dispatch adapters.","title":"Phase 64 — src/ restructured into subdirectories"},{"category":"section","location":"changelog/#Phase-65-—-physical-units-(Unitful-weak-dependency-extension)","page":"Changelog","text":"import Unitful, UnitfulAngles, UnitfulAstro loads UnitfulExt, which maps a column's QuantumUnits keyword onto a Unitful unit:\n\ncolumnunit(t, \"CHAN_FREQ\")   # u\"Hz\"\nqcolumn(t, \"UVW\")            # the whole column as `… m` quantities (materialised)\n\nUnitfulAngles supplies casacore's angle vocabulary (arcsec, mas, °), UnitfulAstro the astronomy units (Jy, pc, AU), and the extension registers the dimensionless \"pseudo-units\" casacore uses that no Julia package provides — beam, pixel, channel, count, adu, lambda, klambda (so Jy/beam parses to Jy beam⁻¹). The string-normalisation layer (_normalize_unit, always loaded) handles casacore's .-as-multiply, %/%%, and the FITS/long-form aliases.\n\nMeasurementSets.UNITS_NO_JULIA_COUNTERPART documents every casacore unit without a third-party Julia implementation and how it is handled:\n\nkind units handling\n:pseudo beam pixel channel count adu lambda klambda registered dimensionless by the extension\n:nounits _, \"\" Unitful.NoUnits\n:unsupported WU FU fu cy deg_2 sq_deg clear error + suggested replacement\n:dimension rad sr dimensionless here; base dimensions in casacore\n\nDivergence from casacore: angles are SI-dimensionless (dimension(u\"rad\") == NoDims) — UnitfulAngles gives the unit names and all angle↔angle / angle↔scalar conversions, but not casacore's treatment of angle as a base dimension. DimensionfulAngles.jl is the strict alternative.\n\nRead-side only for now. TaQL unit literals (3km, 10arcsec) and writing QuantumUnits from Unitful-typed columns are follow-ups.","title":"Phase 65 — physical units (Unitful weak-dependency extension)"},{"category":"section","location":"changelog/#Phase-66-—-measures-/-reference-frames-(SOFA-weak-dependency-extension)","page":"Changelog","text":"A measure-valued column declares its physical quantity and reference frame in a MEASINFO keyword. src/measures/ (always loaded) parses it and reads a cell as a typed value:\n\nmeasinfo(t, \"CHAN_FREQ\")           # MeasInfo(:frequency, …, VarRefCol = \"MEAS_FREQ_REF\", …)\nmeasure(main, \"TIME\", 1)           # MEpoch{UTC}(60454.4… d)\nmeasure(subtable(ms, \"FIELD\"), \"PHASE_DIR\", 1)   # MDirection{J2000}(…, …)\n\nimport SOFA loads SOFAExt (pure-Julia SOFA.jl v2, IAU SOFA port), which converts between frames; import EarthOrientation additionally loads EarthOrientationExt for IERS ΔUT1 / polar motion:\n\nfr = MeasFrame(epoch = measure(main, \"TIME\", 1),\n               position = measure(subtable(ms, \"ANTENNA\"), \"POSITION\", 1),\n               direction = measure(subtable(ms, \"FIELD\"), \"PHASE_DIR\", 1))\n\nmeasconvert(measure(main, \"TIME\", 1), TAI)                 # epoch:  UTC → TAI\nmeasconvert(MDirection{J2000}(2.0, 0.5), AZEL; frame = fr) # direction\nmeasconvert(MFrequency{TOPO}(100e9), LSRK; frame = fr)     # frequency (radio/relativistic Doppler)\n\nEpoch UTC/TAI/TT/TDB/UT1; direction J2000/ICRS/ B1950/APP/GALACTIC/ECLIPTIC/AZEL/AZELGEO/HADEC/ITRF; frequency TOPO/GEO/BARY/LSRK/LSRD/GALACTO.\nVelocity-frame constants copied verbatim from casacore MeasTable.cc; every conversion cross-checked against casatools.measures() (epoch < 1 µs, direction < 5″, frequency < 1 Hz on 100 GHz).\nWithout EarthOrientation.jl: ΔUT1 = 0, no polar motion (~1″), a one-time warning. J2000 is treated as ICRS (~0.02″ frame bias).\n\nWrite path: write_table(dir, name, cols; measures = Dict(\"D\" => (; kind = :direction, ref = \"J2000\"))) (or the per-row (; kind, varrefcol, tabtypes, tabcodes) form) stamps a MEASINFO keyword; copyms / copytable round-trip it verbatim.\n\nNon-goals: solar-system-body direction frames (SUN/MOON/planets), MeasComet / ephemeris tables, MBaseline / MEarthMagnetic, standalone MDoppler, RefOff application, pulsar-timing-grade precision, TaQL measures functions, in-place MEASINFO edit.","title":"Phase 66 — measures / reference frames (SOFA weak-dependency extension)"},{"category":"section","location":"changelog/#Phase-67-—-GitHub-/-CI-wiring-for-the-JuliaAstro-repository","page":"Changelog","text":"The package moved to github.com/JuliaAstro/MeasurementSets.jl. The stale Paul Barrett owner in the README CI badge and the docs/make.jl repo / edit links were fixed, and the git remote re-pointed.\n\n.github/workflows/CI.yml gains a docs job (julia-actions/julia-docdeploy) — builds the Documenter site and deploys it (stable + dev + PR previews) via the DOCUMENTER_KEY secret that TagBot.yml already references — plus a coverage upload on the test job (julia-actions/julia-processcoverage + codecov/codecov-action).\n.github/workflows/CompatHelper.yml added (the canonical Julia [compat]-bumping bot, root + docs/).\n.github/dependabot.yml trimmed to the github-actions ecosystem only — CompatHelper now owns the Julia dependency updates.\nREADME badge rows: docs-stable / docs-dev on the first, CI / codecov / MIT-license on the second.\ndocs/make.jl deploydocs guarded on CI, with versions = [\"stable\" => \"v^\", \"v#.#\", \"dev\" => \"dev\"], matching the JuliaAstro convention.\n\nNo source or test changes; the suite is unchanged at 2591.","title":"Phase 67 — GitHub / CI wiring for the JuliaAstro repository"},{"category":"section","location":"changelog/#Performance-—-bulk-column-reads-in-the-query-engine-and-measure","page":"Changelog","text":"Two hot paths were re-decoding storage-manager cells one at a time inside a per-row loop:\n\nmeasure(t, col) (whole-column) re-parsed the MEASINFO keyword and re-resolved the frame every row, and read the value cell by cell. Now it parses once and bulk-reads the value column (and the VarRefCol code column) via the column's own whole-column fast path.\nquery / groupby / update! / delete! loaded the columns a WHERE / GROUP BY / SET-RHS touches as lazy Columns and indexed col[i] per row — for an IncrementalStMan column (most MAIN metadata: TIME, FIELD_ID, SCAN_NUMBER, …) that re-walks the bucket index on every access. A new _load_col materialises each referenced scalar column once before the loop (array-valued columns stay lazy, so a predicate over a big cube column still streams). Orders-of-magnitude faster on a real-sized table; bounded extra memory (a WHERE usually references 1–3 scalar columns).\n\nNo API or behaviour change.","title":"Performance — bulk column reads in the query engine and measure"},{"category":"section","location":"changelog/#Performance-—-tiled-reads-no-longer-box-every-element","page":"Changelog","text":"reinterpret(T, ::Vector{UInt8}) followed by per-element indexing is an allocating slow path in Julia when sizeof(T) > 1 (each access boxes a ReinterpretArray value). Every TiledStMan read went through it:\n\ngetcell on a DATA cell allocated ~144 KB (the cell is 2 KB); column(t, \"DATA\")[:] allocated ~22 KB/cell.\n\nread_plane / read_cube_whole / _read_cube_bulk now copy each contiguous on-disk run with unsafe_load over a pinned pointer (_rd_run!, handling Real and Complex endianness). Measured on the fixture MS: DATA getcell 144 KB → ~3 KB/cell, UVW 2.0 → 0.9 KB, DATA[:] bulk 22 → 2.2 KB/cell. The RefTable / partial-rows= materialise path (always per-cell) benefits directly.\n\nAlso: the tile-layout computation (sortperm + allocation) is memoised per tileshape on the TiledStMan instead of recomputed on every read_plane; fixed-size index vectors became NTuples; and the SOFA extension uses StaticArrays SVector for its 3-vector / rotation math (StaticArrays added as a weak dependency — it is already a transitive dependency of SOFA.jl, so import SOFA still activates the extension).\n\nNo API, return-type, or behaviour change. 2592 tests.","title":"Performance — tiled reads no longer box every element"},{"category":"section","location":"changelog/#Phase-69-—-TaQL-lite-quantity-literals-date/time-and-angle-functions","page":"Changelog","text":"The string expression grammar (shared by query / groupby / join / update! / VirtualTaQLColumn CALC) gains two loosely-coupled features.\n\nQuantity literals. 1.4GHz, 10arcsec, 30deg — a number token immediately followed (no space) by a unit. Compared against a column that carries a QuantumUnits keyword, the comparison goes through Unitful: query(spw, \"CHAN_FREQ > 1.4GHz\"). When an expression uses a quantity literal, every referenced unit-bearing column is loaded with its unit attached, so a bare-number comparison against such a column then raises a DimensionError (casacore's \"units do not conform\" — a deliberately stricter reading; only bites when bare and unit literals are mixed against one column). A computed select / CALC expression whose result is a dimensionless quantity (\"CHAN_FREQ / 1GHz\") is stripped to a plain number; a dimensional result errors. An update! SET RHS that evaluates to a quantity is converted to the target column's unit. Needs the Unitful extension (import Unitful, UnitfulAngles, UnitfulAstro); the parser errors clearly without it.\n\nDate/time + angle functions (pure, no weak-dep gating — Dates stdlib + trig). Every date value is an MJD Float64 (days), so _bcast / isless / ORDER BY all keep working:\n\ndatetime('2020-02-12') / datetime() / mjd(x) / mjd() / mjdtodate(x) / date(x) / time(x)\nyear / month / day / week / weekday / dow\ncdate / ctime / cmonth / cdow / ctod (formatted strings)\nhms(rad) / dms(rad) — sexagesimal strings\nnormangle(rad) — wrap to (−π, π]\nangdist / angdistx — great-circle distance, angdist(lon1, lat1, lon2, lat2) or angdist([lon1, lat1], [lon2, lat2])\n\nAlso: general array literals [a, b, c] in any expression position (not just IN); scientific-notation number literals (1.4e9, 1e-9) — previously an \"unknown column\" error.\n\nNon-goals: measure literals / a TQLMeasure node (casacore core TaQL has none); mscal.azel() / measures-frame functions; the multi-point (2N-element) angdist array form; a BigFloat / Float128 high-precision MJD variant (considered, deferred); the spaced postfix 3 km unit form (no-space 3km only); input angle literals (30d15m as a value — only the hms(rad) function).\n\n2638 tests.","title":"Phase 69 — TaQL-lite quantity literals + date/time & angle functions"},{"category":"section","location":"changelog/#Phase-70-—-write-QuantumUnits-/-MEASINFO-from-typed-columns","page":"Changelog","text":"The write side of Phases 65-66/69, previously asymmetric: a write_table column could only get a QuantumUnits / MEASINFO keyword from an explicit measures = Dict(...) kwarg with hand-written unit strings; a column whose Julia element type was already a Unitful.Quantity or a Measure hit a bare MethodError.\n\nNow such a column is automatically stored as plain numbers on disk with the right keyword stamped, so write_table → readtable → qcolumn / measure round-trips:\n\nA Unitful.Quantity column (scalar or array-cell) is ustripped to its first element's unit and gets QuantumUnits stamped. A new _ms_ustring (in UnitfulExt) is the inverse of _ms_uparse — a curated reverse-alias map + a round-trip check; a unit with no casacore spelling errors clearly (symmetric with the read side).\nA Measure column — MEpoch{R} → mjd*86400 s, MDirection{R} → [lon,lat] rad, MPosition{R} → [x,y,z] m, MFrequency{R} → Hz, MRadialVelocity{R} → m/s — gets MEASINFO (type + fixed Ref from the first row's frame) + QuantumUnits.\nNew units = Dict(col => \"Hz\" | [\"rad\",\"rad\"]) kwarg on write_table stamps QuantumUnits without a MEASINFO. An explicit measures= / units= entry for a column overrides the auto-detection (wins silently).\n_casatype_of gains an Any fallback with an actionable message (import Unitful / check the eltype) instead of a MethodError.\nedit's addcolumn!(t, name, data) does the same detection.\ncopyms / write_ms unchanged — keyword records are already copied verbatim.\n\nNon-goals: deriving a per-row VarRefCol from a mixed-frame Measure column (fixed Ref only); non-canonical stored units for a Measure; _ms_ustring for the full casacore unit vocabulary (curated\n\nverified, else a clear error); reading a plain column back as a\n\nQuantity automatically (column still returns plain numbers — use qcolumn / measure).\n\n2677 tests.","title":"Phase 70 — write QuantumUnits / MEASINFO from typed columns"},{"category":"section","location":"changelog/#Phase-71-—-MRadialVelocity-reference-frame-conversion","page":"Changelog","text":"Phase 66 shipped measconvert for epoch / direction / frequency and left MRadialVelocity (m/s, SOURCE.SYSVEL) as a clear-error stub. Phase 71 implements it, reusing the frequency path's velocity machinery in ext/SOFAExt.jl (_v_earth_bary / _v_obs_geo / _n_hat / the _VEL_LSRK/_VEL_LSRD/_VEL_LSRGAL constants):\n\nmeasconvert(MRadialVelocity{LSRK}(2e4), BARY; frame = fr)   # needs frame.direction\n\nFrames LSRK / LSRD / BARY / GEO / TOPO / GALACTO, hub = BARY, relativistic velocity addition (β_out = (β_in ∓ g)/(1 ∓ β_in·g), g = V·n̂/c) — matches casacore MCRadialVelocity.\nVerified against casatools.measures(): BARY / LSRD / GALACTO to < 1 mm/s; GEO / TOPO to ~0.25 m/s (the SOFA epv00 vs casacore-ephemeris residual, the same one the frequency test carries as rtol = 2e-9). SOFA-only LSRK → R → LSRK round-trips to 1e-12.\n_MEAS_ENUM[:radialvelocity] added so a bare-code VarRefCol column decodes.\n\nNon-goals: LGROUP / CMB velocity frames (no singletons — a measconvert targeting one errors clearly; no real MS SYSVEL uses them); a standalone MDoppler measure or an MFrequency ↔ MRadialVelocity bridge given a rest frequency (a distinct feature).\n\n2699 tests.","title":"Phase 71 — MRadialVelocity reference-frame conversion"},{"category":"section","location":"changelog/#Phase-72-—-MDoppler-measure-frequency-velocity-bridge","page":"Changelog","text":"The Phase 71 non-goal: the Doppler-shift value — its convention (RADIO / OPTICAL / RATIO / BETA / GAMMA), orthogonal to the reference frame. Pure algebra (c is the only constant, exact SI) → core, no import SOFA.\n\nd = MDoppler{RADIO}(0.01)\nmeasconvert(d, OPTICAL)                        # convention -> convention\n\nν₀ = 1.42040575e9                              # HI rest frequency\ndoppler(MFrequency{LSRK}(1.4e9), ν₀)           # -> MDoppler{BETA}\nfrequency(d, ν₀)                               # MDoppler + rest -> MFrequency{LSRK}\nrestfrequency(MFrequency{LSRK}(1.4e9), d)      # -> MFrequency{REST}\nradialvelocity(d)                              # -> MRadialVelocity{LSRK}  (c·β)\ndoppler(MRadialVelocity{LSRK}(3e5))            # -> MDoppler{BETA}         (β = v/c)\n\nmeasconvert(::MDoppler, C2) uses casacore MCDoppler's hub = RATIO (F = ν/ν₀) route formulas; BETA = RELATIVISTIC = v/c, Z = OPTICAL.\ndoppler / frequency / radialvelocity / restfrequency mirror casacore MFrequency::to{Doppler,Rest} / fromDoppler / MRadialVelocity::{to,from}Doppler — the frame of a fromDoppler result defaults to LSRK (casacore's choice).\n:doppler MEASINFO read + write (_MEAS_FRAMES / _MEAS_ENUM / _measure_column_spec) — completeness; no real MS stores one.\nVerified against casatools.measures() to rtol = 1e-12 (casatools reports every doppler m0 as value·c in \"m/s\" — the fixture divides it back out).\n\nNon-goals: MDoppler reference-frame handling (frame-agnostic — measconvert the frequency first); MDoppler::shiftFrequency (an array helper); a VarRefCol :doppler write path.\n\n2725 tests.","title":"Phase 72 — MDoppler measure + frequency ↔ velocity bridge"},{"category":"section","location":"changelog/#Phase-73-—-shared-constants-file","page":"Changelog","text":"The physical / astronomical / calendar constants that were repeated across src/ and the extensions (speed of light — a bare literal in Phase 72 and SOFA.LIGHTSPEED in the SOFA extension; seconds- and milliseconds-per-day; the MJD reference epoch; one arcsecond; the AU; the casacore LSR-motion velocity vectors) are now defined once in src/constants.jl — MeasurementSets.C_LIGHT, SEC_PER_DAY, MSEC_PER_DAY, MJD_JD_OFFSET, MJD_EPOCH, ARCSEC, AU_METRES, VEL_LSRK / VEL_LSRD / VEL_LSRGAL. src/measures/, src/taql/, ext/SOFAExt.jl and ext/EarthOrientationExt.jl reference these (the extensions keep short local aliases). Every value is an exact SI / IAU definition and was verified equal to the SOFA.jl constant it replaced — behaviour-preserving, test count unchanged. Format-internal magic numbers (Dysco bit widths, CTDS type codes, container header offsets) stay with their subsystems.\n\n2725 tests.","title":"Phase 73 — shared constants file"},{"category":"section","location":"changelog/#Phase-74-—-shiftfreq-one-step-frequency-velocity-bridges","page":"Changelog","text":"The Phase 72 non-goals — casacore MDoppler::shiftFrequency and the spectral-axis array forms:\n\nshiftfreq(d, νs)                              # νs .* √((1−β)/(1+β))\nradialvelocity(f::MFrequency, ν₀)            # = radialvelocity(doppler(f, ν₀))\nfrequency(v::MRadialVelocity, ν₀)            # = frequency(doppler(v), ν₀)\n\nradialvelocity.(measure(spw, \"CHAN_FREQ\", 1), ν₀)   # a whole velocity axis\n\nshiftfreq(d, ν) — ν a Hz number, an MFrequency (frame label kept), or a vector of either (the Doppler factor is computed once). Unlike casacore it converts a non-BETA d to BETA first.\nThe two new radialvelocity / frequency methods compose the Phase 72 pieces so the common MFrequency ↔ MRadialVelocity step is one call; they broadcast, so no dedicated Vector method is needed for the bridges (only shiftfreq has one, for the factor-once path).\n_beta_factor extracted and reused by frequency / restfrequency.\n\n2735 tests.","title":"Phase 74 — shiftfreq + one-step frequency ↔ velocity bridges"},{"category":"section","location":"changelog/#Phase-75-—-MBaseline-MuvW-vector-measures","page":"Changelog","text":"The UVW column and antenna-to-antenna baselines as typed measures. Both are 3-vectors (metres) in a direction frame (ITRF default); measconvert rotates them.\n\nmeasure(main, \"UVW\", 1)                        # -> MuvW{ITRF}(u, v, w m)\nmeasconvert(MBaseline{ITRF}(x, y, z), J2000; frame = fr)\nmeasconvert(measure(main, \"UVW\", 1), J2000; frame = fr)   # needs fr.direction\n\nwrite_table(dir, \"T\", [\"W\" => [MuvW{J2000}(10.0, 20.0, 30.0)]]; nrow = 1)\n\nmeasure now reads a type = \"uvw\" column as MuvW (was MPosition) and a type = \"baseline\" column as MBaseline (was an error). Both frame sets mirror MDirection::Types.\nMBaseline conversion = convert the unit direction via the existing MDirection code, rescale by the original length — matches casacore MCBaseline for every route (pure rotation, or its adjust/readjust for the aberration routes).\nMuvW conversion adds the phase-centre pole rotation (MCuvw::toPole / fromPole, R = RotMatrix(Euler(-π/2+lat, 2, -lon, 3))), so it also needs frame.direction.\nWriting is automatic for an MBaseline / MuvW column (kind :baseline / :uvw, [\"m\",\"m\",\"m\"]); copyms round-trips the keyword.\nNon-goal (unchanged): MEarthMagnetic, the IGRF field model, MVuvw(baseline, direction) construction, a VarRefCol uvw/baseline write path.","title":"Phase 75 — MBaseline + MuvW vector measures"},{"category":"section","location":"changelog/#Phase-76-—-solar-system-body-direction-reference-frames","page":"Changelog","text":"A FIELD.PHASE_DIR column can name a solar-system body as its frame (a moving target). Nine body singleton frames — SUN MOON MERCURY VENUS MARS JUPITER SATURN URANUS NEPTUNE — resolved via SOFA.plan94 / moon98 (no new dependency).\n\nmeasure(fld, \"PHASE_DIR\", 1)                          # -> MDirection{SUN}(0.0, 0.0)\nmeasconvert(MDirection{SUN}(0, 0), AZEL; frame = fr)  # the Sun's az/el\nmeasconvert(MDirection{JUPITER}(0, 0), J2000; frame = fr)\n\nmeasure() reads a body-frame column as MDirection{SUN} (the stored (lon, lat) is a placeholder); a per-row VarRefCol code ≥ 32 decodes through _BODY_ENUM (40 => SUN, 41 => MOON, …).\nmeasconvert computes the body's geocentric astrometric direction (light-time iterated), routed through the existing _icrs_to_dir chain for the target frame. The Moon's topocentric parallax is applied when frame.position is set (~1° for the Moon, ≲30″ planets).\nAccuracy is the plan94 / moon98 floor: ~arcsec for Sun / Moon / Venus / Mercury, ~20-40″ for Mars, ~arcmin for Jupiter / Saturn — fine for a pointing / scheduling reference, not astrometry.\nBody frames are source-only: measconvert(d, SUN) errors.\nNon-goals: PLUTO (no plan94), COMET / MeasComet ephemeris tables, a stored pointing offset in a body-frame cell, solar light deflection (< 2″, below the ephemeris floor).","title":"Phase 76 — solar-system-body direction reference frames"},{"category":"section","location":"changelog/#Phase-77-—-mscal.*-derived-MS-TaQL-functions","page":"Changelog","text":"The astronomy-value subset of casacore's derivedmscal UDFs in the TaQL-lite grammar — per-MAIN-row values from TIME + the ANTENNA / FIELD subtables via the Measures engine (needs import SOFA).\n\nquery(main, \"mscal.el1() > 0.35\")                       # elevation cut\ngroupby(main, \"FIELD_ID\"; select = [\"az\" => \"gmean(mscal.az1())\"])\nquery(main, \"\"; select = [\"w\" => \"mscal.uvw_j2000()\", \"d\" => \"mscal.delay()\"])\n\nFunctions: mscal.ha1() / ha2() / ha() (hour angle, rad); mscal.hadec1() ([ha, dec]); mscal.azel1() ([az, el]); mscal.az1() / el1() (scalar); mscal.pa1() (parallactic angle); mscal.last1() (local apparent sidereal time, rad — casacore returns a raw MVEpoch day count here; we return the angle); mscal.itrf() (PHASE_DIR in ITRF, [lon, lat]); mscal.uvw_j2000() ([u, v, w] m — the UVW column in J2000); mscal.delay() (geometric delay, s). 1 / 2 picks ANTENNA1 / ANTENNA2; no suffix uses antenna 0.\nThe tokenizer already lexed mscal.ha1 as one identifier (Phase 63); a TQLMScal AST node + a precompute hook in _tql_cols (covers query + groupby) and _vtq_prepare! (VirtualTaQLColumn). Angle functions memo by (antenna, field, TIME).\nCross-checked against real derivedmscal: ha1 to < 1″, pa1 / last1 to < 1″ (pa1 uses casacore's HADEC-pole, not the J2000 pole).\nNon-goals: the CASA-MSSelection selection functions (mscal.baseline / mscal.spw / …), the Observatories-table array centre, mscal.* in a query closure / a join on string / on a GroupedTable.","title":"Phase 77 — mscal.* derived-MS TaQL functions"},{"category":"section","location":"changelog/#Phase-78-—-mscal.stokes()-polarization-conversion","page":"Changelog","text":"mscal.stokes(col [, 'types'] [, rescale]) — a TaQL-lite function that converts a DATA / FLAG / WEIGHT array cell between correlation bases, a port of casacore's StokesConverter.\n\nquery(main, \"any(abs(mscal.stokes(DATA, 'I')) > 5.0)\")\ngroupby(main, \"FIELD_ID\"; select = [\"p\" => \"gmean(mean(abs(mscal.stokes(DATA, 'I'))))\"])\n\ntypes (default 'IQUV') is an alias (IQUV / STOKES, CIRC / CIRCULAR, LIN / LINEAR) or a comma-list of Stokes / circular / linear names ('I', 'I,V', 'XX,YY'). Mixed-hand RX..YL outputs are not supported.\nInput basis comes from POLARIZATION.CORR_TYPE row 1 (one setup per MS, as casacore's UDF does). The result is a (nOut, nchan) matrix; a (ncorr,) WEIGHT / SIGMA cell yields a (nOut,) vector.\nPer-cell: a matrix multiply for Complex data (out[o,ch] = Σⱼ conv[o,j]·in[j,ch]), an any-of-contributing test for Bool flags, the weight-propagation formula for Float weights. The 6 basis-change 4×4 matrices are hardcoded (no LinearAlgebra); rescale applies casacore's 0.5 fudge factor to codes 5–12.\nThreading mirrors mscal.*: a TQLStokes node, a \"::stokes::…\" sentinel ref split by _stokes_split in _tql_cols (covers query + groupby) and _vtq_prepare!.\nCross-checked against real derivedmscal mscal.stokes to rtol = 1e-5.\nNon-goals: RX..YL input frames; the Ptotal / Plinear / … pseudo outputs; per-DATA_DESC_ID CORR_TYPE; mscal.stokes in a query closure / join on string.","title":"Phase 78 — mscal.stokes() polarization conversion"},{"category":"section","location":"changelog/#Phase-79-—-mscal.uvw_j2000()-rotation-memo","page":"Changelog","text":"The ITRF→J2000 uvw transform is a linear map (pole rotation + baseline rotation) that depends only on the frame — i.e. on (antenna, field, TIME). Phase 77 evaluated it with one measconvert(::MuvW) per MAIN row; Phase 79 memoizes the 3×3 (as its three result columns, three measconverts per distinct key) and applies it to each row's stored UVW. On a real MAIN this is one matrix per integration-baseline pair instead of one per row. No behaviour change — cross-checked against a direct per-row measconvert to rtol = 1e-9.","title":"Phase 79 — mscal.uvw_j2000() rotation memo"},{"category":"section","location":"changelog/#Phase-80-—-mscal.sel()-MSSelection-lite-selection-functions","page":"Changelog","text":"The derivedmscal row-selection UDFs in the TaQL-lite grammar — mscal.baseline / mscal.field / mscal.spw / mscal.scan / mscal.state / mscal.array / mscal.obs, each mscal.<sel>('spec') returning a per-MAIN-row Bool.\n\nquery(main, \"mscal.baseline('ea01 & *') AND mscal.field('3C286')\")\nquery(main, \"mscal.spw('0~3') AND NOT mscal.scan('1,2')\")\n\nspec is a comma-list of terms; a row passes if it matches ANY. Terms: N, N~M (inclusive range), >N / <N / >=N / <=N, an exact name, a glob (* ? [...]), or /regex/ — names matched against the type's NAME column (ANTENNA.NAME, FIELD.NAME, SPECTRAL_WINDOW.NAME, STATE.OBS_MODE). A !-prefixed term is subtracted (a spec of only !-terms selects everything else).\nmscal.baseline additionally: L & R / L && R (baseline between two antenna sets — & drops autocorrelations, && keeps them; L & repeats L), and a whole-spec ! negation. spw maps the row's DATA_DESC_ID through DATA_DESCRIPTION.SPECTRAL_WINDOW_ID.\nA hand-written comma-list parser (not a port of casacore's per-type yacc grammars). Threads like mscal.* — a TQLMSSel node, a \"::mssel::<fn>::<spec>\" sentinel split by _mssel_split in _tql_cols / _vtq_prepare!.\nCross-checked against real derivedmscal (row counts match).\nNon-goals: mscal.time / mscal.uvdist; channel sub-selection on spw (0:5~20); mscal.corr / mscal.feed.","title":"Phase 80 — mscal.<sel>() MSSelection-lite selection functions"},{"category":"section","location":"changelog/#Phase-81-—-mscal.time()-/-mscal.uvdist()-selection","page":"Changelog","text":"The last two derivedmscal selection UDFs.\n\nquery(main, \"mscal.time('2024/05/24/10:00:00~2024/05/24/11:00:00')\")\nquery(main, \"mscal.uvdist('20~200klambda') AND NOT mscal.uvdist('<50m')\")\n\nmscal.time('spec') — a comma-list of t0~t1 ranges (or >t0 / <t1). Each endpoint is an ISO (2024-05-24T10:00:00) or YYYY/MM/DD[/HH:MM:SS] datetime (parsed by the Phase-69 _tql_parse_datetime), or a bare number = MJD days. Compared against the MAIN TIME column (UTC seconds).\nmscal.uvdist('spec') — a comma-list of a~b ranges (or >a / <b) with a trailing unit: m (default) / km / lambda / klambda / mlambda. Uses the 2-D uv-distance √(u²+v²) (casacore's fast path); wavelength units scale per row by the row's spw SPECTRAL_WINDOW.REF_FREQUENCY. A spec must not mix distance and wavelength units; a bare single value (no range/bound) is an error.\nBoth extend _mssel_one in src/taql/mscal.jl; threading unchanged (the \"::mssel::<fn>::<spec>\" sentinel from Phase 80).\nuvdist row counts cross-checked against real derivedmscal; mscal.time is hand-computed only (casacore's time-string defaults make an exact edge match fiddly).\nNon-goals: casacore's full time grammar (* wildcards, + durations, [...] edge buffers, MS-derived field defaults); the :P% percent-tolerance on a uvdist value.","title":"Phase 81 — mscal.time() / mscal.uvdist() selection"},{"category":"section","location":"changelog/#Phase-89-—-Aqua.jl-quality-checks","page":"Changelog","text":"New test/aqua_tests.jl runs Aqua.test_all (ambiguities, undefined exports, stale deps, compat bounds, unbound type parameters, project extras, persistent tasks — all clean). Base.delete! / Base.insert! (the taql-style write commands, Phases 30-31, which accept a path string) are whitelisted for the type-piracy check.\nFilled in the missing [compat] bounds Aqua flagged — Dates, Mmap, Random (stdlibs), and the test-only Aqua / Casacore / CxxWrap / Test extras. Every dependency now carries a bound.\nREADME gains an Aqua badge. The CI workflow (Phase 67) already runs the full test suite, so Aqua runs on every PR.\n\nmeasconvert for MFrequency / MRadialVelocity now handles the two remaining casacore velocity frames:\n\nLGROUP — the Local Group barycentre (casacore MeasTable::calcVelocityLGROUP, 308 km/s).\nCMB — the cosmic-microwave-background rest frame (the dipole, F. Ghigo's 369.5 km/s toward the galactic-coordinate direction).\n\nVEL_LGROUP / VEL_CMB added to src/constants.jl; the LGROUP_BARY / CMB_BARY routes in ext/SOFAExt.jl are the same Doppler-shift form as LSRK↔BARY. Round-trip verified; the constants are copied verbatim from MeasTable.cc (no casatools oracle for these two).\n\nAlso: corrected the stale \"RefOff is parsed-and-carried\" comment — it is ignored (no standard MS column carries one).\n\nquery(main, \"PHASE_DIR[1] > 10h30m AND PHASE_DIR[2] BETWEEN 40d AND 50d\")\n\nThe tokenizer recognises a <number><unit> run whose unit is a sexagesimal token (h / h30m / h30m15s → hour angle, d / d51m / d51m16 → degrees) and emits a :num token with the value in radians (via the Phase-86 _parse_sexagesimal). _sexagesimal_unit does the classification.\nA unit that isn't a sexagesimal token (30deg, 1.4GHz, 10m) stays a Phase-69 quantity literal — no regression.\n10h now means \"10 hours of hour angle = 150°\", not a 10-hour duration (matches casacore TaQL); use arithmetic on seconds for a duration.\n\nquery(main, \"mscal.el1('10h42m31.3, 45d51m16')\")     # a J2000 direction\nquery(t, \"abs(RA - angle('10h30m')) < 0.01\")         # angle() in any expression\n\n_parse_sexagesimal(s, kind) (src/taql/functions.jl) — kind ∈ :ra (h/m/s time, ×15 → degrees) / :dec (d/m/s degrees). Accepts 10h42m31.3s, 10:42:31.3, 10 42 31.3, a leading sign, or a bare decimal (degrees). Returns radians.\nA new angle('...') TaQL-lite function — sexagesimal string → radians (h in the string ⇒ hour angle).\nmscal.* direction argument (Phase 85) now also accepts a comma-separated sexagesimal 'RA, DEC' string, in addition to the [ra, dec] radian pair.\nNon-goal: a bare sexagesimal literal in the grammar (WHERE RA > 10h30m) — needs a tokenizer + node change; angle('10h30m') covers it.\n\nThe Phase-77 direction functions (ha / hadec / azel / az / el / pa / itrf / delay) take an optional direction argument instead of FIELD.PHASE_DIR, mirroring casacore's derivedmscal help text.\n\nquery(main, \"mscal.el1('SUN') > 0.35\")               # elevation of the Sun\nquery(main; select = [\"az\" => \"mscal.az1('DELAY_DIR')\"])\nquery(main, \"mscal.hadec1([2.0, 0.5])\")              # a fixed J2000 direction\n\nThe argument is a solar-system body name ('SUN' … 'NEPTUNE', 'MOON'), a FIELD direction column ('PHASE_DIR' / 'DELAY_DIR' / 'REFERENCE_DIR' — ephemeris-aware via Phase 82), or a [ra, dec] J2000 pair in radians. No argument → FIELD.PHASE_DIR (unchanged).\nmscal.last (sidereal time) and mscal.uvw_j2000 are direction-intrinsic and stay 0-argument.\nTQLMScal gains a dir field; the sentinel is \"mscal.<fn>::<dir>\". _mscal_columns resolves the per-row J2000 direction (_djfor) and keys its frame-conversion memo by (antenna, direction, TIME).","title":"Phase 89 — Aqua.jl quality checks"},{"category":"section","location":"changelog/#Phase-84-—-mscal.corr()-/-mscal.feed()-selection","page":"Changelog","text":"The two remaining derivedmscal selection UDFs, completing the mscal.* selection set.\n\nquery(main, \"mscal.corr('RR,LL')\")           # rows whose pol setup has RR or LL\nquery(main, \"mscal.feed('0 & 1') AND mscal.baseline('DA*')\")\n\nmscal.corr('spec') — a comma-list of correlation names (RR / XX / I / … via the Phase-78 _STOKES_NAMES) or integer Stokes codes. A per-row Bool: true if the row's polarization setup (POLARIZATION.CORR_TYPE via DATA_DESCRIPTION.POLARIZATION_ID) shares any code with the request.\nmscal.feed('spec') — the mscal.baseline antenna-grammar form on FEED1 / FEED2 (L & R feed-pair, L && R, !, comma-lists of ids / N~M ranges), with numeric feed ids only.\nBoth in src/taql/mscal.jl — _parse_corr_types, _mssel_one corr / feed branches (feed reuses _mssel_baseline_pred).\nmscal.corr / mscal.feed are not registerUDF-registered in the casacore build here (only advertised in the help text), so the real-derivedmscal cross-check skips them cleanly.\n\nmscal.spw('spec') now takes the MSSelection spwid:chanlist form, and a companion mscal.chan('spec') returns the per-row channel mask.\n\nquery(main, \"mscal.spw('0:5~20')\")                    # spw 0, chans 5-20 non-empty\nquery(main, \"any(mscal.chan('0:100~200MHz'))\")        # rows with a channel in the band\nquery(main; select = [\"m\" => \"mscal.chan('0:5~20;40~50')\"])   # per-row BitVector\n\nspec is a comma-list of <spwterm>[:<chanlist>]. <spwterm> is a single MSSelection-lite term (N / N~M / >N / <N / a name / glob / /regex/ against SPECTRAL_WINDOW.NAME / *). <chanlist> is a ;-list of a (single 0-based index), a~b (inclusive range), a~b^step, f1~f2GHz (a CHAN_FREQ range, Hz/kHz/MHz/GHz), or <f / >f.\nmscal.spw stays a per-row Bool — a channelled spw matches only if at least one of the row's channels is selected (so 0:100~200 on a 64-channel spw excludes the row). mscal.chan returns a per-row BitVector of the spw's channel count (all-false for an unselected spw), for any(...) / count(...) / a Phase-60 masked array.\nAll in src/taql/mscal.jl — _parse_spw_spec / _parse_chan_elem / _chan_mask; _mssel_one's spw branch gains the channelled path, new chan branch. Threading unchanged.\nmscal.spw('0:5~20') row count cross-checked against real derivedmscal.\nNon-goals: velocity units on a channel range (casacore disables them too); ^step on a frequency range; per-DATA_DESC_ID distinct channel geometry within one spw.","title":"Phase 84 — mscal.corr() / mscal.feed() selection"},{"category":"section","location":"changelog/#Phase-82-—-solar-system-ephemeris-(MeasComet)-tables","page":"Changelog","text":"A moving-target FIELD row — a non-negative EPHEMERIS_ID with a matching EPHEM<id>_*.tab polynomial position table — now gets its time-dependent direction from that table, closing the Phase-76 plan94-accuracy gap for comets and planets.\n\ne = field_ephemeris(subtable(ms, \"FIELD\"), 0)          # nothing if not a moving target\nd = ephemeris_direction(e, 60454.4)                    # MDirection at that MJD\nmeasure(fld, \"PHASE_DIR\", 1; epoch = MEpoch{UTC}(60454.4))\nquery(main, \"mscal.hadec1()\")                          # uses the ephemeris automatically\n\nNew src/measures/ephemeris.jl: Ephemeris struct + open_ephemeris (reads the MJD0 / dMJD / NAME / posrefsys keywords + MJD / RA / DEC / Rho / RadVel columns), ephemeris_direction / _radvel / _distance (linear interpolation of the (ρ,RA,Dec) Cartesian vector between bracketing rows — casacore's MeasComet::get), and field_ephemeris(fld, field_id) (globs EPHEM<id>_*.tab in the FIELD directory).\nmeasure(t, col, row; epoch) — new kwarg; for a direction cell of a FIELD table with an ephemeris it returns the ephemeris position (shifted by the stored PHASE_DIR offset).\n_mscal_columns (mscal.*) _fielddir — an ephemeris field is evaluated per (field, TIME) (converted to TDB), then to J2000.\nwrite_table gains a keywords::AbstractDict kwarg for scalar / string-array table-level keywords (used to build ephemeris fixtures).\nVerified by an open_ephemeris / ephemeris_direction round-trip and an end-to-end mscal.hadec1() on a copied MS with a patched moving-target FIELD. No casatools oracle (needs a real ephemeris table + me.framecomet — documented gap).\nNon-goals: polynomial (numpoly > 0) PHASE_DIR interpolation; the DiskLong / DiskLat sub-Earth point; the aipsrc measures.comet.directory lookup; MeasComet as a MeasFrame for a COMET-coded direction column outside FIELD.","title":"Phase 82 — solar-system ephemeris (MeasComet) tables"},{"category":"section","location":"#MeasurementSets.jl","page":"Home","text":"A pure-Julia reader and writer for the Measurement Set version 2 data format — the casacore Table Data System (CTDS) tables used for interferometric visibility data by ALMA, the VLA, LOFAR and others.\n\nNo C-library dependencies. HDF5.jl (a wrapper over the C libhdf5) is an optional weak dependency, loaded via a package extension only when you import HDF5 to read or write a MultiHDF5 (table.mfh5) container.","title":"MeasurementSets.jl"},{"category":"section","location":"#Installation","page":"Home","text":"The package is not yet registered. Add it by path or URL:\n\nusing Pkg\nPkg.develop(path = \"/path/to/MeasurementSets.jl\")   # or Pkg.add(url = \"...\")\n\nThen:\n\nusing MeasurementSets\nms = MeasurementSet(\"/path/to/my.ms\")","title":"Installation"},{"category":"section","location":"#At-a-glance","page":"Home","text":"Read — every storage manager a real MS uses, decoded in pure Julia: StandardStMan, IncrementalStMan, the three Tiled*StMan (single- and multi-column hypercubes), DyscoStMan (lossy compression), and the virtual scaling / compression engines (ScaledArrayEngine, CompressComplex, …). MultiFile / MultiHDF5 container tables, and RefTable / ConcatTable (TaQL selections, MultiMS MAIN), too. Columns are lazy AbstractVectors; tables are Tables.jl sources (DataFrame(subtable(ms, \"ANTENNA\"))).\n\nWrite — write_table / create_ms / copyms / copytable create conformant tables, preserving each column's storage-manager / engine / compression kind (or choosing one via tsm= / ism= / engines= / dysco= / storage= kwargs). Verified byte-for-byte against casacore.\n\nEdit in place — edit(path) do t … end: overwrite cells / whole columns, addrows!, removerows!, addcolumn!, removecolumn!.\n\nConcurrency — cooperative fcntl locking + table.lock sync-blob row-count tracking, so a table is safe to share with another Julia session or a live casa / python-casacore process (is_stale / resync / is_multiused).\n\nQuery — a small TaQL-like engine: query (WHERE with arithmetic, LIKE / regex, a function library, ORDER BY, projection → a RefTable), groupby (GROUP BY + g* aggregates + HAVING), join (an N:1 lookup join), and the row-level write commands update! / delete! / insert! / SELECT … INTO, plus a taql(\"…\") string dispatcher. Results chain — each is an AbstractTable.\n\nSchema — the MS v2 standard schema (NRAO Memo 229) as data (SCHEMAVER2, stdtable, stdcolumns); validate(ms) checks a table against it.","title":"At a glance"},{"category":"section","location":"#Contents","page":"Home","text":"Pages = [\"concepts.md\", \"guide.md\", \"api.md\", \"changelog.md\"]\nDepth = 2","title":"Contents"}]
}
